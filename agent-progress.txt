RALPH LOOP — AGENT PROGRESS
===========================

Project: note-taking app (Next.js 16 + Bun + bun:sqlite + better-auth + TipTap).
Source of truth: SPEC.MD (design) + CLAUDE.md (conventions). Task list: prd.json.
State note: repo was reset to a bare Next.js skeleton ("reset to default skeleton
for ralph loop experiment"), so CLAUDE.md's "auth + DB foundation done" no longer
holds — every prd task starts from scratch. Building bottom-up: db -> auth -> data
layer -> actions -> UI -> tests.

--------------------------------------------------------------------------------
DONE
--------------------------------------------------------------------------------

[database] Create SQLite database with better-auth schema — passes=true
  - VERIFY-ONLY task (no new code): the full schema is already created on cold
    start. lib/db.ts's openDatabase() mkdir's data/ + opens app.sqlite + creates
    the `notes` table; importing lib/auth.ts runs better-auth's migrations
    (getMigrations -> runMigrations) which create user/session/account/
    verification. This task just confirms it and flips passes=true. COMPLETES the
    `database` category (all 9 db tasks now passes=true).
  - VERIFIED: throwaway bun script (_verify-schema.ts, removed after) imported
    lib/auth (triggering the real migration path), then inspected the live DB via
    sqlite_master + PRAGMA table_info/foreign_key_list. ALL CHECKS PASS:
      * 5 tables present: user, session, account, verification, notes.
      * user cols: id, name, email, emailVerified, image, createdAt, updatedAt.
      * session cols incl. userId + token + expiresAt; session.userId -> user FK.
      * account (credential provider) cols incl. userId, providerId, accountId,
        password (hash lives here, never in user — SPEC §5.1).
      * verification cols: id, identifier, value, expiresAt.
      * notes.user_id -> user FK present.
    `bun run build` PASS (4 routes), `bun run lint` PASS (clean).
  - No UI -> Playwright N/A. test/test:e2e unchanged (still pending prd tasks).

[database] Build database access utilities (lib/db.ts)  — passes=true
  - Created lib/db.ts: shared SQLite handle + typed helpers.
  - Singleton getDb() (lazy open, cached) with WAL + foreign_keys pragmas.
  - query<T>(sql, params) -> rows; get<T>(sql, params) -> row|null;
    run(sql, params) -> { changes, lastInsertRowid } (normalized to numbers).
  - Prepared-statement cache; all SQL parameterized via $name binds.
  - closeDb() for test isolation.
  - DESIGN DECISION (spec-sanctioned, SPEC §3.2/§17): thin driver abstraction.
    Uses bun:sqlite under Bun (dev/build/start run with --bun) and falls back to
    node:sqlite (DatabaseSync) under plain Node, so the data layer is testable
    under Vitest (which runs on Node) without breaking production. Driver picked
    at runtime via createRequire + a variable module specifier (bundler-safe).
  - Verified: dual-runtime smoke test (Bun + Node, all asserts pass), tsc via
    `bun run build` (OK), `bun run lint` (clean).

[auth] Configure better-auth server (lib/auth.ts)  — passes=true
  - Created lib/auth.ts: single betterAuth() instance, the authoritative server
    auth surface. emailAndPassword enabled; nextCookies() plugin last (forwards
    Set-Cookie from server-side calls/actions). secret + baseURL auto-read from
    BETTER_AUTH_SECRET / BETTER_AUTH_URL env.
  - DB: passes the SHARED handle from getDb() (SPEC §6 "one file, two consumers").
    Cast to bun:sqlite Database type (driver-agnostic getDb() returns the real
    bun:sqlite instance under the Bun runtime, which better-auth supports natively).
  - Migrations: better-auth never auto-migrates, so on cold start we run them
    programmatically — getMigrations(auth.options) -> runMigrations() (idempotent),
    imported from "better-auth/db/migration". Guarded by NEXT_PHASE !==
    "phase-production-build" so `next build` does no DB work.
  - Verified end-to-end via a throwaway Bun smoke test (since vitest/Node can't
    drive bun:sqlite-backed auth): migrations create all 4 tables (user, session,
    account, verification); getSession(no cookie) -> null; signUpEmail persists a
    user row + an account row (providerId "credential", hash in account not user).
    Smoke file + its DB removed after. `bun run build` PASS (auth.ts typechecks:
    cast + top-level await OK), `bun run lint` PASS.
  - NOTE for next iteration: this ALSO created the better-auth schema, so the prd
    task "Create SQLite database with better-auth schema" is now trivially
    satisfiable — just verify the 4 tables exist and mark it passes=true.

[database] Create notes table with indexes  — passes=true
  - Added applyNotesSchema() to lib/db.ts, called from openDatabase() right after
    the pragmas, so the `notes` table + indexes are created idempotently on cold
    start (CREATE TABLE/INDEX IF NOT EXISTS). This is per CLAUDE.md's decision
    ("notes schema lives in lib/db.ts"); supersedes the earlier "db.ts creates no
    tables" stance from the lib/db.ts iteration.
  - Schema (SPEC §5.2 shape, prd column names): id TEXT PRIMARY KEY NOT NULL,
    user_id TEXT NOT NULL REFERENCES "user"(id) ON DELETE CASCADE, title TEXT,
    content_json TEXT NOT NULL, is_public INTEGER NOT NULL DEFAULT 0,
    public_slug TEXT UNIQUE, created_at/updated_at TEXT NOT NULL.
    Indexes: idx_notes_user_id, idx_notes_is_public; public_slug is indexed by
    its UNIQUE autoindex (the share-link lookup path) — no separate named index.
  - NAMING DECISION: used prd's content_json / public_slug (not SPEC §5.2's
    content / public_id). The prd defines `passes` and is internally consistent
    across the data-layer tasks (Note type uses camelCase contentJson/publicSlug,
    and there's a getNoteByPublicSlug task). TS Note type (next task) will map
    snake_case rows -> camelCase. Flagging so a future iter doesn't "fix" this.
  - FORWARD-FK / ORDERING (corrects the note below about user existing first):
    by design getDb() (-> openDatabase -> applyNotesSchema) runs BEFORE auth's
    migrations create `user` (lib/auth.ts calls getDb() then runs migrations).
    SQLite resolves REFERENCES at write time, not DDL time, so creating `notes`
    with a forward FK to a not-yet-existing `user` is fine; the FK becomes
    enforceable once auth creates `user`. Verified end-to-end under bun:sqlite
    AND node:sqlite with the real better-auth migration path.
  - VERIFIED: 17-check throwaway bun script (all 8 cols + types/NOT NULL,
    is_public DEFAULT 0, FK enforced on insert, ON DELETE CASCADE, UNIQUE rejects
    dup public_slug + allows many NULLs, all 3 index targets present, id NOT NULL
    rejects NULL). Adversarial 3-lens review (compliance/robustness/invariants)
    all voted "ship", nits only. `bun run build` PASS, `bun run lint` PASS.

[database] Define Note type and create helper functions (lib/notes.ts) — passes=true
  - Created lib/notes.ts: the data-layer boundary that maps raw `notes` rows to
    the app-level Note. Scoped to THIS task (types + helpers); the repository
    query/mutation fns (createNote/getNoteById/.../setNotePublic) are later tasks
    and will be appended to this same file.
  - `Note` type: camelCase (userId/contentJson/isPublic/publicSlug/createdAt/
    updatedAt). `contentJson` is the PARSED TipTap doc object (typed JSONContent
    from @tiptap/core, type-only import — erased at compile, no client coupling),
    not the stored string. `isPublic` is a real boolean. title/publicSlug nullable.
  - `NoteRow` type: the raw snake_case SQLite shape (content_json: string,
    is_public: 0|1) — matches lib/db.ts's `notes` schema exactly.
  - `mapRowToNote(row)`: snake->camel, is_public 0/1 -> boolean, JSON.parse of
    content_json -> object.
  - `EMPTY_TIPTAP_DOC`: { type:"doc", content:[{ type:"paragraph" }] } (SPEC §9.2);
    deep-frozen so the shared constant can't be mutated — clone before editing.
  - `generatePublicSlug()`: nanoid(21) — URL-safe, high-entropy, unguessable
    (SPEC §10.1/§14.5; prd asked for nanoid 16+ chars, 21 exceeds it).
  - DEP: promoted `nanoid` from a transitive dep to a DIRECT dependency
    (`bun add nanoid` -> nanoid@5.1.16, ESM; works under Bun + Node/Vitest).
  - DESIGN: did NOT add `import "server-only"` (package not installed AND it
    throws when imported under plain Node, which would break the Vitest tests
    that import this module — consistent with lib/db.ts's node:sqlite fallback).
  - VERIFIED: 12-assert Bun smoke test (doc shape + frozen, slug length 21 /
    url-safe / unique, full row->Note mapping incl. 0->false). `bun run build`
    PASS (typechecks), `bun run lint` PASS.

[database] Implement createNote repository function — passes=true
  - Appended createNote() to lib/notes.ts (first repository fn; mapper/helpers
    from the previous iter live in the same file). Imports `run` from lib/db.
  - Signature: `async createNote(userId, data?: CreateNoteInput): Promise<Note>`
    where CreateNoteInput = { title?: string; contentJson?: JSONContent }.
    Async per the prd wording (the later get/update/delete repo fns stay sync —
    DB driver is sync; the actions layer wraps all of them).
  - Behavior: id = crypto.randomUUID() (global in Bun + Node); created_at ===
    updated_at = new Date().toISOString(); is_public 0 / public_slug NULL
    (private until shared). Defaults: title -> "Untitled note" (prd; note SPEC
    §5.4 prefers nullable/derived — prd wins here), content -> structuredClone of
    EMPTY_TIPTAP_DOC (clone so the returned doc is mutable; the shared const
    stays frozen). Content stored as JSON.stringify (never raw HTML, SPEC §5.3).
  - INSERT is parameterized ($name binds) and scoped to user_id = userId. Returns
    the Note constructed from the inserted values (no extra round-trip SELECT).
  - VERIFIED: 22-assert Bun smoke test (DATABASE_PATH=:memory:, stub `user` table
    + row for the FK): uuid id, user scoping, both defaults, mutable returned
    content, isPublic/publicSlug, equal timestamps, row persisted snake_case +
    stringified content round-trips, custom title/content, unique ids, FK rejects
    unknown user, EMPTY_TIPTAP_DOC still frozen after call. All 22 pass under Bun.
    (Node node:sqlite path not re-smoked: throwaway harness used bare specifiers
    Node ESM won't resolve — code is driver-agnostic; Vitest will cover Node.)
    `bun run build` PASS, `bun run lint` PASS. Smoke file removed.

[database] Implement getNoteById and getNotesByUser functions — passes=true
  - Appended getNoteById() + getNotesByUser() to lib/notes.ts (sync reads, per
    the repo-fn convention: driver is sync; the actions layer wraps them). Added
    `get` + `query` to the existing `import { run } from "./db"`.
  - getNoteById(userId, noteId): SELECT * FROM notes WHERE id=$id AND
    user_id=$userId -> mapRowToNote(row) or null. Ownership is baked into the
    WHERE clause, so "not found" and "owned by someone else" are
    indistinguishable — both null. Callers translate null -> notFound() (404,
    not 403) so existence isn't revealed (SPEC §8, §14.2).
  - getNotesByUser(userId): SELECT * FROM notes WHERE user_id=$userId ORDER BY
    updated_at DESC -> Note[] (rows.map(mapRowToNote)). Scoped to the owner;
    newest-edited first to drive the dashboard listing.
  - All SQL parameterized ($id/$userId binds). Reuses get()/query() from lib/db
    and mapRowToNote() from lib/notes (no new SELECT-and-map duplication).
  - VERIFIED: 14-assert throwaway Bun smoke test (DATABASE_PATH=:memory:, stub
    `user` table + alice/bob rows). Covers: owner fetch returns mapped Note
    (id/title/parsed contentJson/isPublic), AUTHZ — bob gets null for alice's
    note + missing id gets null, per-user list scoping (alice 2 / bob 1, no
    cross-leak), empty array for a user with no notes, and ORDER BY updated_at
    DESC verified deterministically via explicit distinct timestamps (createNote
    ms-resolution timestamps can tie). 14/14 pass under Bun. Smoke file removed.
    `bun run build` PASS (typechecks), `bun run lint` PASS.

[database] Implement updateNote function — passes=true
  - Appended updateNote() to lib/notes.ts (sync, per the repo-fn convention:
    reads/update/delete stay sync; driver is sync; the actions layer wraps them).
    Added `type QueryParams` to the existing `import { ... } from "./db"`.
  - Signature: updateNote(userId, noteId, data: UpdateNoteInput): Note | null
    where UpdateNoteInput = { title?: string | null; contentJson?: JSONContent }.
    `undefined` field = leave untouched; title accepts null to clear it (column
    is nullable, SPEC §5.4).
  - PARTIAL UPDATE: builds the SET clause from only the provided fields. The
    assignment fragments ("title = $title", "content_json = $contentJson") are a
    FIXED, hardcoded set — no user input is ever interpolated into SQL; every
    value is still bound via $name params (SPEC §14.3 / CLAUDE.md invariant).
    Flagging so a future iter doesn't "fix" the `${assignments.join()}` as if it
    were injection — it's column names, not user data.
  - updated_at = new Date().toISOString() is ALWAYS bumped (even on an empty
    patch) so dashboard ORDER BY updated_at DESC reflects the edit.
  - AUTHZ baked into WHERE (id=$id AND user_id=$userId): a missing or
    someone-else's note updates 0 rows; the fn then RE-READS via getNoteById
    (same ownership scope) and returns null — never revealing existence
    (404-not-403 story, SPEC §8/§14.2). Re-read also guarantees the returned
    Note mirrors the persisted row.
  - VERIFIED: 19-assert throwaway Bun smoke test (DATABASE_PATH=:memory:, stub
    `user` table + alice/bob). Covers: title-only (content untouched, updated_at
    bumped > created_at, created_at unchanged, persisted), content-only (changed,
    title kept, stored as JSON *string*), both fields, title->null clear,
    AUTHZ (bob gets null + alice's note unchanged), missing id -> null, empty
    patch (returns Note, bumps updated_at, keeps fields). 19/19 pass under Bun.
    Smoke file removed. `bun run build` PASS, `bun run lint` PASS.
  - REPO HAZARD NOTED (not introduced by this task): the PostToolUse format hook
    runs `bun run format` (oxfmt), which CANNOT parse the Next.js skeleton JSX in
    app/page.tsx and can transiently corrupt it — one `bun run build` failed on a
    mangled page.tsx, but a re-run against the on-disk (valid) file PASSED. If a
    future build fails on app/page.tsx JSX, re-run; the file itself is fine.

[database] Implement deleteNote function — passes=true
  - Appended deleteNote() to lib/notes.ts (sync, per the repo-fn convention:
    reads/update/delete stay sync; the actions layer wraps them). Reuses `run`
    from the existing lib/db import — no new imports.
  - Signature: deleteNote(userId, noteId): boolean. Single parameterized DELETE
    WHERE id=$id AND user_id=$userId; returns run().changes > 0.
  - AUTHZ baked into WHERE (mirrors updateNote): a missing or someone-else's note
    deletes 0 rows -> returns false, never revealing existence (404-not-403 story,
    SPEC §8/§14.2). Idempotent — re-deleting an already-gone note returns false,
    no throw.
  - VERIFIED: 6-assert throwaway Bun smoke test (DATABASE_PATH=:memory:, stub
    `user` table + alice/bob). Covers: non-owner delete -> false + note untouched,
    missing id -> false, owner delete -> true + note gone (getNoteById null),
    re-delete -> false. 6/6 pass. Smoke file removed.
    `bun run build` PASS (typechecks), `bun run lint` PASS (clean).

[database] Implement note sharing functions — passes=true
  - Appended setNotePublic() + getNoteByPublicSlug() to lib/notes.ts (sync, per
    the repo-fn convention). Reuses get/run from lib/db + getNoteById/
    generatePublicSlug/mapRowToNote already in lib/notes — no new imports. This
    COMPLETES the database/data-layer category.
  - setNotePublic(userId, noteId, isPublic): Note | null. Reads via getNoteById
    first (ownership baked into its WHERE), so a missing/someone-else's note ->
    null with no existence leak (SPEC §14.2). Enable: mint a slug ONLY if
    public_slug is NULL (re-share reuses the link), set is_public=1. Disable: set
    is_public=0 but KEEP the slug. updated_at bumped. Single parameterized UPDATE
    scoped to id+user_id.
  - getNoteByPublicSlug(slug): Note | null. SELECT ... WHERE public_slug=$slug
    AND is_public=1 (no userId — the public route). Gated SOLELY on is_public=1,
    looked up by the share token (never the internal id) — SPEC §10.1/§10.3/§14.5
    + CLAUDE.md invariant. An unshared note that still carries a slug 404s.
  - SPEC-vs-prd CONFLICT RESOLVED: prd step says "clear public_slug when
    disabling"; SPEC §10.2 + CLAUDE.md invariant say KEEP it (re-share yields the
    same URL; lookup gates on is_public=1 so old links 404 while unshared). SPEC
    is authoritative (CLAUDE.md) -> kept the slug. Documented in the fn docstring
    so a future iter doesn't "fix" it back to clearing.
  - VERIFIED: 18-assert throwaway Bun smoke test (DATABASE_PATH=:memory:, stub
    `user` table + alice/bob). Covers: enable mints 21-char slug + flips public +
    bumps updated_at, public lookup finds it (content maps), unknown slug -> null,
    disable keeps slug but public lookup 404s, re-enable REUSES same slug + lookup
    works again, AUTHZ (bob/missing -> null, alice's note untouched). 18/18 pass.
    Smoke file removed. `bun run build` PASS, `bun run lint` PASS (clean).

[auth] Create better-auth API route — passes=true
  - Created app/api/auth/[...all]/route.ts: the single catch-all that serves
    EVERY auth endpoint (sign-up/sign-in/sign-out/session/...). Per SPEC §7.2:
    `export const { GET, POST } = toNextJsHandler(auth)` (better-auth/next-js).
    Followed SPEC over the prd's "use auth.handler" wording — toNextJsHandler is
    the idiomatic App Router wrapper and internally uses auth.handler.
  - RISKY-INTEGRATION FIX (the real work this iter): adding the route pulled
    lib/auth.ts -> lib/db.ts into a BUILT route bundle for the first time, which
    exposed that Turbopack can't handle lib/db.ts's dynamic driver require. It
    statically rewrote `nodeRequire(specifier)` into a build-time throw stub
    ("Cannot find module as expression is too dynamic"), failing
    `next build` at "Collecting page data for /api/auth/[...all]". The
    `turbopackIgnore` magic comment did NOT help (Turbopack doesn't honor it on a
    createRequire alias). Earlier `bun run build` PASSes were illusory — nothing
    imported lib/auth/db into a route until now, so the dynamic require was never
    bundled.
  - RESOLUTION: replaced createRequire + dynamic require in lib/db.ts with
    `process.getBuiltinModule(specifier)`. It's a plain runtime method call (not
    require/import/require.resolve/new Worker), so Turbopack leaves it untouched.
    Verified BOTH runtimes load their driver: bun:sqlite under Bun
    (process.getBuiltinModule('bun:sqlite').Database), node:sqlite under Node 24
    for Vitest (.DatabaseSync). Removed the now-unused createRequire/node:module
    import. Driver-agnostic dual-runtime design preserved (CLAUDE.md/SPEC §3.2).
  - VERIFIED: `bun run build` PASS (/api/auth/[...all] registered as ƒ dynamic),
    `bun run lint` PASS, dual-runtime db smoke (Bun + Node both load + query).
    Live endpoint checks vs the dev server: GET /api/auth/ok -> {"ok":true} 200;
    GET /api/auth/get-session (no cookie) -> null 200; POST /api/auth/sign-up/
    email -> 200 with a persisted user + token (full integration through the
    shared getDb() handle, incl. the new getBuiltinModule driver path).

[auth] Implement server-side auth helpers — passes=true
  - Added getSession() + requireAuth() to lib/auth.ts (per prd step "Export
    helpers from lib/auth.ts" — NOT lib/session.ts; prd wins on file path). These
    are the authoritative server-side session surface (SPEC §7.5); the optimistic
    middleware cookie check is never the real gate.
  - getSession(): Promise<Session> — wraps auth.api.getSession({ headers: await
    headers() }) from next/headers; returns { user, session } | null. Validated
    server-side (not just cookie-presence). Exported a `Session` type alias
    (Awaited<ReturnType<typeof auth.api.getSession>>) so callers get the shape.
  - requireAuth(): Promise<NonNullable<Session>> — getSession() then redirect()s
    to "/authenticate" when null (prd's auth route is /authenticate, not SPEC's
    /login). redirect()'s `never` return narrows the result to a guaranteed
    non-null Session, so protected pages/actions can use session.user.id directly.
  - ROUTE NOTE: redirect target is /authenticate and protected dashboard is
    /dashboard — the prd's route names (app/authenticate, app/dashboard,
    app/notes/[id], app/p/[slug]) differ from SPEC §11 (/login, /notes,
    /share/[publicId]). prd is what `passes` tracks; following prd routes.
  - Imports next/headers + next/navigation at module top. lib/auth.ts is already
    server-only (top-level await migrations, better-auth/next-js), so no new
    client coupling; build confirms it still bundles into the auth route.
  - VERIFIED: 7-assert throwaway Bun smoke (DATABASE_PATH=:memory:) of the
    underlying auth.api.getSession the helpers wrap — no cookie -> null, sign-up
    issues Set-Cookie, valid cookie -> session with correct user.email/name +
    token, bogus cookie -> null (server-validated, not presence-only). 7/7 pass.
    (The helpers themselves need Next's request scope so can't be called from a
    bare Bun script; build typechecks them + the smoke proves the wrapped API.)
    Smoke file removed. `bun run build` PASS (helpers bundle into /api/auth route),
    `bun run lint` PASS.

[auth] Create auth client for frontend — passes=true
  - Created lib/auth-client.ts: the browser-side auth surface for "use client"
    components (SPEC §7.3). `export const authClient = createAuthClient()` from
    "better-auth/react" — baseURL OMITTED on purpose: app + /api/auth share one
    origin, so the client defaults to the current origin and talks to the
    catch-all handler (app/api/auth/[...all]/route.ts).
  - Per prd "Export signIn, signUp, signOut + useSession": added named re-exports
    via `export const { signIn, signUp, signOut, useSession } = authClient` for
    ergonomic call sites (signIn.email / signUp.email / signOut() / useSession()).
    Confirmed names against current better-auth/react docs (context7).
  - NO lib/db / lib/auth import here, so no Turbopack dynamic-require concern; this
    module is client-safe and bundles cleanly.
  - VERIFIED: smoke import under Bun — exports authClient, signIn, signOut, signUp,
    useSession; signIn.email is a function, useSession is a function. `bun run lint`
    PASS (clean), `bun run build` PASS (compiled + TypeScript ran + 4 static pages).
    No UI in this task -> Playwright N/A. test/test:e2e unchanged (see below).
  - UNBLOCKS auth UI: SignUpForm/LoginForm/LogoutButton can now import signUp/
    signIn/signOut/useSession from "@/lib/auth-client". Next suggested: the auth
    UI forms -> app/authenticate page -> Header + LogoutButton.

[ui] Build sign up form component — passes=true   (FIRST UI task; opens UI phase)
  - Created components/SignUpForm.tsx: "use client" controlled form (name/email/
    password via useState — minimal state, no useEffect). Submits through the
    better-auth browser client `signUp.email({ name, email, password })` from
    @/lib/auth-client (the catch-all handler persists the user). Confirmed the
    `{ data, error }` return shape + error.message via context7 (better-auth docs).
  - COMPONENT API: props `{ onSuccess?: () => void }`. Per the prd step "Handle
    success (will redirect in parent)" the form does NOT navigate itself — it
    calls onSuccess on success and the parent (app/authenticate, a later task)
    owns the redirect to /dashboard. Flagging so a future iter doesn't add a
    router.push here.
  - CLIENT-SIDE VALIDATION (fast feedback only; server stays authoritative per
    SPEC §13): name non-empty, email matches a pragmatic regex, password >= 8
    (MIN_PASSWORD_LENGTH = better-auth's emailAndPassword default). On failure
    sets an inline error and returns BEFORE calling the server. Server errors are
    surfaced from error.message into the same <p role="alert"> region.
  - A11y/modern React: semantic <form> + <label htmlFor>/id pairs, autoComplete
    (name/email/new-password), required + minLength, role="alert" error, inputs+
    button disabled while pending ("Creating account…"). noValidate so our custom
    messages show instead of native bubbles. Tailwind v4 utilities, foreground/
    background theme tokens (dark-mode aware).
  - VERIFIED with Playwright MCP against the LIVE dev server (existing instance on
    :3000 — derisks the flagged Windows Bun+Next dev-server risk; it serves fine).
    Drove a TEMP host route app/signup-smoke/page.tsx (rendered <SignUpForm
    onSuccess={() => show "SIGNUP_OK"} />), then REMOVED it (deliverable is just
    the component): (1) form renders Name/Email/Password + Sign up; (2) invalid
    email -> inline alert "Please enter a valid email address." (no server call);
    (3) valid submit -> real better-auth signup succeeds -> onSuccess fires ->
    "SIGNUP_OK" rendered; (4) 0 console errors. `bun run build` PASS (back to 3
    routes after temp removal), `bun run lint` PASS.
  - NOTE: full signup writes a user row to the dev DB data/app.sqlite (gitignored)
    — used a throwaway unique email. Harmless.
  - NEXT: components/LoginForm.tsx (same pattern, signIn.email, no name field) ->
    app/authenticate/page.tsx (toggles SignUpForm/LoginForm, redirects to
    /dashboard on success) -> Header (server, getSession) + LogoutButton.

[ui] Build login form component — passes=true
  - Created components/LoginForm.tsx: "use client" controlled form, a near-mirror
    of SignUpForm (same Tailwind styling, a11y, error region, pending handling)
    but WITHOUT the name field and signing in via signIn.email({ email, password })
    from @/lib/auth-client. Same `{ error }` return-shape handling.
  - COMPONENT API: props `{ onSuccess?: () => void }`. Per prd step "Handle success
    (will redirect in parent)" the form does NOT navigate itself — calls onSuccess;
    the parent (app/authenticate, next task) owns the /dashboard redirect. Flagging
    so a future iter doesn't add router.push here.
  - CLIENT-SIDE VALIDATION (fast feedback only; server authoritative, SPEC §13):
    email matches the same pragmatic regex as SignUpForm, password non-empty (no
    min-length on LOGIN — length is a sign-up rule; an existing account's password
    is whatever it is, and the server rejects wrong creds anyway). On failure sets
    an inline error and returns BEFORE the server call. Server errors surfaced from
    error.message into the same <p role="alert"> region.
  - A11y: semantic <form>, <label htmlFor>/id pairs, autoComplete email +
    current-password (vs SignUpForm's new-password), required, role="alert",
    inputs+button disabled while pending ("Signing in…"), noValidate.
  - VERIFIED with Playwright MCP against the LIVE dev server (:3000): drove a TEMP
    host route app/login-smoke/page.tsx (<LoginForm onSuccess={() => show
    "LOGIN_OK"} />), then REMOVED it (deliverable is just the component). Seeded a
    real user via POST /api/auth/sign-up/email first. Checks: (1) renders Email/
    Password/Log in; (2) invalid email -> inline alert "Please enter a valid email
    address." (no server call); (3) VALID creds -> real better-auth sign-in
    succeeds -> onSuccess fires -> "LOGIN_OK" rendered, 0 console errors; (4) WRONG
    password -> server error surfaced inline "Invalid email or password" (the only
    console error is the expected 401 network response, not a code bug).
    `bun run build` PASS (3 routes after temp removal), `bun run lint` PASS.
  - NEXT: app/authenticate/page.tsx (toggles SignUpForm/LoginForm, redirects to
    /dashboard on success) -> Header (server, getSession) + LogoutButton.

[ui] Create authentication page — passes=true
  - Created app/authenticate/page.tsx: "use client" page that hosts BOTH forms
    and toggles between them IN PLACE (no navigation). useState<Mode> ("login" |
    "signup"); the page owns ONLY the toggle + post-success redirect — each form
    still owns its own input state/validation/error display (SignUpForm/LoginForm
    already built). Renders LoginForm or SignUpForm conditionally; switching mode
    remounts the other form so its state (incl. password) resets — desirable.
  - REDIRECT: forms call onSuccess -> handleSuccess does router.push("/dashboard")
    + router.refresh() (next/navigation). refresh() makes server components re-read
    the session cookie that better-auth's nextCookies() set during the client auth
    call. /dashboard is a LATER prd task so it 404s for now — the redirect itself
    is correct; the 404 is expected until the dashboard page exists.
  - SCOPE NOTE: did NOT add "redirect away if already authed" (SPEC §11 mentions it
    for /login,/signup, but it's NOT a prd step here AND /dashboard doesn't exist
    yet). That belongs to the (app) layout / middleware auth-gate task. Flagging so
    a future iter doesn't think it's missing.
  - STYLE: centered card (flex min-h-screen items-center justify-center), max-w-sm
    column, mode-aware <h1> ("Welcome back" / "Create your account") + subtitle,
    bottom toggle line ("Don't have an account? Sign up" <-> "Already have an
    account? Log in") as a real <button>. Uses foreground/background theme tokens
    (text-foreground/60 opacity modifiers work — colors registered in @theme).
    Semantic <main>/<header>/<h1>, dark-mode aware.
  - VERIFIED with Playwright MCP against the LIVE dev server (:3000): (1) /authenticate
    renders in LOGIN mode (Welcome back + Email/Password/Log in); (2) clicking the
    "Sign up" toggle swaps to SIGNUP mode (Create your account + Name/Email/Password/
    Sign up + "Already have an account? Log in"); (3) filling a fresh unique email +
    submitting -> real better-auth signup succeeds -> onSuccess fires -> URL changes
    to /dashboard. The ONLY console errors are the expected /dashboard 404s (page is
    a later task), NOT a code bug. `bun run build` PASS (/authenticate registered as
    a static ○ route, 4 routes total), `bun run lint` PASS (clean).
  - NOTE: signup wrote a user row to dev DB data/app.sqlite (gitignored) via a
    throwaway unique email (ralph-authpage-0628@example.com). Harmless.
  - NEXT: Header (server component, getSession()) + LogoutButton ("use client",
    signOut) -> then the ACTIONS layer (lib/actions/notes.ts "use server") which
    unblocks the dashboard/editor/share UI.

[actions] Create server action for creating notes — passes=true
  (FIRST actions task; opens the ACTIONS layer + adds lib/validation.ts. Chose
  this foundation over the Header/LogoutButton UI tasks per the loop rule
  "foundation/risky before routine UI" — actions unblock the whole CRUD UI.)
  - Created lib/validation.ts (SPEC §13 — the shared Zod surface for all
    mutations; client is never trusted): TitleSchema (z.string().trim().max(200)),
    NoteContentSchema (z.looseObject requiring type:"doc" + content array, .refine
    caps serialized size at MAX_CONTENT_BYTES=1MB via TextEncoder byte count),
    CreateNoteSchema ({ title?, contentJson? } both optional). Used Zod v4 API
    (z.looseObject, not deprecated .passthrough()) — verified vs zod 4.4.3 via
    context7. looseObject KEEPS extra top-level doc keys (e.g. attrs) instead of
    silently dropping them. Did NOT deep-validate node types (editor whitelist in
    lib/tiptap.ts handles that, a later task) — shape + size only, defense in depth.
  - Created lib/actions/notes.ts ("use server"): exports type ActionResult<T> =
    { ok:true; data:T } | { ok:false; error:string } (serializable result so
    client callers can react to failures; will be reused by update/delete/share
    actions) + createNoteAction(input?). Guard order session -> Zod (only when
    input supplied) -> SQL (createNote repo) -> revalidatePath("/dashboard").
  - DECISION: action uses getSession() and RETURNS an error result for the
    unauthenticated case (not requireAuth()'s thrown redirect). Matches prd
    "Return created note or error" + keeps the action serializable/testable; the
    client form decides how to recover. requireAuth() stays the gate for PAGES.
    Flagging so a future iter doesn't swap it to requireAuth here.
  - With no/empty input the repo applies defaults ("Untitled note" + empty TipTap
    doc). Parsed contentJson cast to JSONContent when handed to createNote.
  - VERIFIED: `bun run build` PASS (TS typechecks both new files; build graph
    unchanged — 4 routes — since nothing imports the action yet), `bun run lint`
    PASS. 13-assert throwaway Bun smoke of lib/validation (empty ok, title trim +
    max + non-string reject, doc shape accept/reject, loose elements + extra keys
    preserved, 1MB oversize reject, both-fields) — 13/13 pass, file removed.
    The action wiring (getSession/revalidatePath) needs a Next request scope so
    it isn't bare-script testable; full e2e arrives with CreateNoteButton. No UI
    in this task -> Playwright N/A.
  - NEXT (actions, in file order): updateNoteAction (ownership + UpdateNoteSchema +
    revalidate), deleteNoteAction, toggleShareAction. Then dashboard/editor/share
    UI consume these. Reuse ActionResult<T> + add UpdateNoteSchema to lib/validation.

--------------------------------------------------------------------------------
CHECKS STATUS (whole repo)
--------------------------------------------------------------------------------
  bun run build     : PASS
  bun run lint      : PASS
  bun run test      : FAIL (expected) — vitest IS now installed (test script =
                      `vitest run`, + jsdom/testing-library in devDeps), but no
                      *.test/*.spec files exist yet so it exits 1 "No test files
                      found". Pre-existing, not a regression (prd tasks "Write
                      unit tests for ..." still pending — they author the files).
  bun run test:e2e  : N/A — no test:e2e script defined; Playwright not installed
                      (prd task "Set up Playwright for E2E testing" still pending).

--------------------------------------------------------------------------------
NEXT / NOTES FOR FUTURE ITERATIONS
--------------------------------------------------------------------------------
  - DATABASE CATEGORY FULLY COMPLETE (all 9 db tasks passes=true): data layer
    (lib/notes.ts) full repository surface (create/getById/getByUser/update/
    delete/setNotePublic/getNoteByPublicSlug) + types/helpers, AND the schema
    task (verified this iter — user/session/account/verification + notes all
    created on cold start). AUTH CATEGORY ALSO COMPLETE (server, route, helpers,
    client all passes=true). Next phases: auth UI -> actions -> CRUD/editor/share
    UI -> tests.
  - AUTH ROUTE DONE this iter (app/api/auth/[...all]/route.ts) — endpoints live
    and verified. Remember the Turbopack lesson: any NEW route/page that imports
    lib/db (directly or via lib/auth) must build clean; the getBuiltinModule
    driver load already handles that, so don't "fix" it back to a dynamic require.
  - Suggested next (in rough dependency order) — db + auth foundation now ALL
    done, so the frontier is auth UI then actions then the rest of the UI:
      1. AUTH UI: components/SignUpForm.tsx + components/LoginForm.tsx ("use
         client", import signUp/signIn from "@/lib/auth-client", controlled
         state, client-side validation, error display) -> app/authenticate/page.tsx
         (toggles the two forms, redirects to /dashboard on success) -> Header
         (server component, getSession()) + LogoutButton ("use client", signOut).
         NOTE Turbopack: any NEW route/page importing lib/db (directly or via
         lib/auth, e.g. Header) must build clean — getBuiltinModule already
         handles it, don't revert to a dynamic require.
      2. ACTIONS: lib/actions/notes.ts ("use server") wrapping the lib/notes repo
         fns; guard order session -> ownership -> Zod -> SQL -> revalidatePath.
         These unblock CreateNoteButton, the editor save/delete, and ShareToggle.
      3. THEN CRUD/editor/share UI (dashboard, /notes/[id] editor + TipTap,
         /p/[slug] public viewer) and finally the test tasks (Vitest + Playwright).
         Server helpers DONE: getSession()/requireAuth() in lib/auth.ts;
         requireAuth() redirects to /authenticate (prd route).
  - DONE this iter: "Implement server-side auth helpers" — getSession()/
    requireAuth() in lib/auth.ts (see DONE section). requireAuth redirects to
    /authenticate (prd route), returns NonNullable<Session>.
  - REPO-FN CONVENTION (set by createNote this iter): createNote is async per prd;
    the read/update/delete/share fns can stay SYNC (driver is sync). All return
    Note | Note[] | boolean and bake ownership into the WHERE clause. Reuse
    run()/get()/query() from lib/db and mapRowToNote() from lib/notes.
  - lib/db.ts deliberately does NOT create any tables; auth tables come from
    better-auth migrations (now wired in lib/auth.ts on cold start).
  - When wiring the notes table, follow SPEC §5 (notes columns) and reuse
    getDb()/run().
  - Vitest runs under Node, so keep server data code driver-agnostic (the
    node:sqlite fallback in lib/db.ts handles this). NOTE: better-auth itself is
    backed by bun:sqlite under the app runtime; do NOT try to drive auth flows
    from vitest/Node — verify auth via Bun (smoke script or e2e) instead.
