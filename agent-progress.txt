RALPH LOOP — AGENT PROGRESS
===========================

Project: note-taking app (Next.js 16 + Bun + bun:sqlite + better-auth + TipTap).
Source of truth: SPEC.MD (design) + CLAUDE.md (conventions). Task list: prd.json.
State note: repo was reset to a bare Next.js skeleton ("reset to default skeleton
for ralph loop experiment"), so CLAUDE.md's "auth + DB foundation done" no longer
holds — every prd task starts from scratch. Building bottom-up: db -> auth -> data
layer -> actions -> UI -> tests.

--------------------------------------------------------------------------------
DONE
--------------------------------------------------------------------------------

[database] Create SQLite database with better-auth schema — passes=true
  - VERIFY-ONLY task (no new code): the full schema is already created on cold
    start. lib/db.ts's openDatabase() mkdir's data/ + opens app.sqlite + creates
    the `notes` table; importing lib/auth.ts runs better-auth's migrations
    (getMigrations -> runMigrations) which create user/session/account/
    verification. This task just confirms it and flips passes=true. COMPLETES the
    `database` category (all 9 db tasks now passes=true).
  - VERIFIED: throwaway bun script (_verify-schema.ts, removed after) imported
    lib/auth (triggering the real migration path), then inspected the live DB via
    sqlite_master + PRAGMA table_info/foreign_key_list. ALL CHECKS PASS:
      * 5 tables present: user, session, account, verification, notes.
      * user cols: id, name, email, emailVerified, image, createdAt, updatedAt.
      * session cols incl. userId + token + expiresAt; session.userId -> user FK.
      * account (credential provider) cols incl. userId, providerId, accountId,
        password (hash lives here, never in user — SPEC §5.1).
      * verification cols: id, identifier, value, expiresAt.
      * notes.user_id -> user FK present.
    `bun run build` PASS (4 routes), `bun run lint` PASS (clean).
  - No UI -> Playwright N/A. test/test:e2e unchanged (still pending prd tasks).

[database] Build database access utilities (lib/db.ts)  — passes=true
  - Created lib/db.ts: shared SQLite handle + typed helpers.
  - Singleton getDb() (lazy open, cached) with WAL + foreign_keys pragmas.
  - query<T>(sql, params) -> rows; get<T>(sql, params) -> row|null;
    run(sql, params) -> { changes, lastInsertRowid } (normalized to numbers).
  - Prepared-statement cache; all SQL parameterized via $name binds.
  - closeDb() for test isolation.
  - DESIGN DECISION (spec-sanctioned, SPEC §3.2/§17): thin driver abstraction.
    Uses bun:sqlite under Bun (dev/build/start run with --bun) and falls back to
    node:sqlite (DatabaseSync) under plain Node, so the data layer is testable
    under Vitest (which runs on Node) without breaking production. Driver picked
    at runtime via createRequire + a variable module specifier (bundler-safe).
  - Verified: dual-runtime smoke test (Bun + Node, all asserts pass), tsc via
    `bun run build` (OK), `bun run lint` (clean).

[auth] Configure better-auth server (lib/auth.ts)  — passes=true
  - Created lib/auth.ts: single betterAuth() instance, the authoritative server
    auth surface. emailAndPassword enabled; nextCookies() plugin last (forwards
    Set-Cookie from server-side calls/actions). secret + baseURL auto-read from
    BETTER_AUTH_SECRET / BETTER_AUTH_URL env.
  - DB: passes the SHARED handle from getDb() (SPEC §6 "one file, two consumers").
    Cast to bun:sqlite Database type (driver-agnostic getDb() returns the real
    bun:sqlite instance under the Bun runtime, which better-auth supports natively).
  - Migrations: better-auth never auto-migrates, so on cold start we run them
    programmatically — getMigrations(auth.options) -> runMigrations() (idempotent),
    imported from "better-auth/db/migration". Guarded by NEXT_PHASE !==
    "phase-production-build" so `next build` does no DB work.
  - Verified end-to-end via a throwaway Bun smoke test (since vitest/Node can't
    drive bun:sqlite-backed auth): migrations create all 4 tables (user, session,
    account, verification); getSession(no cookie) -> null; signUpEmail persists a
    user row + an account row (providerId "credential", hash in account not user).
    Smoke file + its DB removed after. `bun run build` PASS (auth.ts typechecks:
    cast + top-level await OK), `bun run lint` PASS.
  - NOTE for next iteration: this ALSO created the better-auth schema, so the prd
    task "Create SQLite database with better-auth schema" is now trivially
    satisfiable — just verify the 4 tables exist and mark it passes=true.

[database] Create notes table with indexes  — passes=true
  - Added applyNotesSchema() to lib/db.ts, called from openDatabase() right after
    the pragmas, so the `notes` table + indexes are created idempotently on cold
    start (CREATE TABLE/INDEX IF NOT EXISTS). This is per CLAUDE.md's decision
    ("notes schema lives in lib/db.ts"); supersedes the earlier "db.ts creates no
    tables" stance from the lib/db.ts iteration.
  - Schema (SPEC §5.2 shape, prd column names): id TEXT PRIMARY KEY NOT NULL,
    user_id TEXT NOT NULL REFERENCES "user"(id) ON DELETE CASCADE, title TEXT,
    content_json TEXT NOT NULL, is_public INTEGER NOT NULL DEFAULT 0,
    public_slug TEXT UNIQUE, created_at/updated_at TEXT NOT NULL.
    Indexes: idx_notes_user_id, idx_notes_is_public; public_slug is indexed by
    its UNIQUE autoindex (the share-link lookup path) — no separate named index.
  - NAMING DECISION: used prd's content_json / public_slug (not SPEC §5.2's
    content / public_id). The prd defines `passes` and is internally consistent
    across the data-layer tasks (Note type uses camelCase contentJson/publicSlug,
    and there's a getNoteByPublicSlug task). TS Note type (next task) will map
    snake_case rows -> camelCase. Flagging so a future iter doesn't "fix" this.
  - FORWARD-FK / ORDERING (corrects the note below about user existing first):
    by design getDb() (-> openDatabase -> applyNotesSchema) runs BEFORE auth's
    migrations create `user` (lib/auth.ts calls getDb() then runs migrations).
    SQLite resolves REFERENCES at write time, not DDL time, so creating `notes`
    with a forward FK to a not-yet-existing `user` is fine; the FK becomes
    enforceable once auth creates `user`. Verified end-to-end under bun:sqlite
    AND node:sqlite with the real better-auth migration path.
  - VERIFIED: 17-check throwaway bun script (all 8 cols + types/NOT NULL,
    is_public DEFAULT 0, FK enforced on insert, ON DELETE CASCADE, UNIQUE rejects
    dup public_slug + allows many NULLs, all 3 index targets present, id NOT NULL
    rejects NULL). Adversarial 3-lens review (compliance/robustness/invariants)
    all voted "ship", nits only. `bun run build` PASS, `bun run lint` PASS.

[database] Define Note type and create helper functions (lib/notes.ts) — passes=true
  - Created lib/notes.ts: the data-layer boundary that maps raw `notes` rows to
    the app-level Note. Scoped to THIS task (types + helpers); the repository
    query/mutation fns (createNote/getNoteById/.../setNotePublic) are later tasks
    and will be appended to this same file.
  - `Note` type: camelCase (userId/contentJson/isPublic/publicSlug/createdAt/
    updatedAt). `contentJson` is the PARSED TipTap doc object (typed JSONContent
    from @tiptap/core, type-only import — erased at compile, no client coupling),
    not the stored string. `isPublic` is a real boolean. title/publicSlug nullable.
  - `NoteRow` type: the raw snake_case SQLite shape (content_json: string,
    is_public: 0|1) — matches lib/db.ts's `notes` schema exactly.
  - `mapRowToNote(row)`: snake->camel, is_public 0/1 -> boolean, JSON.parse of
    content_json -> object.
  - `EMPTY_TIPTAP_DOC`: { type:"doc", content:[{ type:"paragraph" }] } (SPEC §9.2);
    deep-frozen so the shared constant can't be mutated — clone before editing.
  - `generatePublicSlug()`: nanoid(21) — URL-safe, high-entropy, unguessable
    (SPEC §10.1/§14.5; prd asked for nanoid 16+ chars, 21 exceeds it).
  - DEP: promoted `nanoid` from a transitive dep to a DIRECT dependency
    (`bun add nanoid` -> nanoid@5.1.16, ESM; works under Bun + Node/Vitest).
  - DESIGN: did NOT add `import "server-only"` (package not installed AND it
    throws when imported under plain Node, which would break the Vitest tests
    that import this module — consistent with lib/db.ts's node:sqlite fallback).
  - VERIFIED: 12-assert Bun smoke test (doc shape + frozen, slug length 21 /
    url-safe / unique, full row->Note mapping incl. 0->false). `bun run build`
    PASS (typechecks), `bun run lint` PASS.

[database] Implement createNote repository function — passes=true
  - Appended createNote() to lib/notes.ts (first repository fn; mapper/helpers
    from the previous iter live in the same file). Imports `run` from lib/db.
  - Signature: `async createNote(userId, data?: CreateNoteInput): Promise<Note>`
    where CreateNoteInput = { title?: string; contentJson?: JSONContent }.
    Async per the prd wording (the later get/update/delete repo fns stay sync —
    DB driver is sync; the actions layer wraps all of them).
  - Behavior: id = crypto.randomUUID() (global in Bun + Node); created_at ===
    updated_at = new Date().toISOString(); is_public 0 / public_slug NULL
    (private until shared). Defaults: title -> "Untitled note" (prd; note SPEC
    §5.4 prefers nullable/derived — prd wins here), content -> structuredClone of
    EMPTY_TIPTAP_DOC (clone so the returned doc is mutable; the shared const
    stays frozen). Content stored as JSON.stringify (never raw HTML, SPEC §5.3).
  - INSERT is parameterized ($name binds) and scoped to user_id = userId. Returns
    the Note constructed from the inserted values (no extra round-trip SELECT).
  - VERIFIED: 22-assert Bun smoke test (DATABASE_PATH=:memory:, stub `user` table
    + row for the FK): uuid id, user scoping, both defaults, mutable returned
    content, isPublic/publicSlug, equal timestamps, row persisted snake_case +
    stringified content round-trips, custom title/content, unique ids, FK rejects
    unknown user, EMPTY_TIPTAP_DOC still frozen after call. All 22 pass under Bun.
    (Node node:sqlite path not re-smoked: throwaway harness used bare specifiers
    Node ESM won't resolve — code is driver-agnostic; Vitest will cover Node.)
    `bun run build` PASS, `bun run lint` PASS. Smoke file removed.

[database] Implement getNoteById and getNotesByUser functions — passes=true
  - Appended getNoteById() + getNotesByUser() to lib/notes.ts (sync reads, per
    the repo-fn convention: driver is sync; the actions layer wraps them). Added
    `get` + `query` to the existing `import { run } from "./db"`.
  - getNoteById(userId, noteId): SELECT * FROM notes WHERE id=$id AND
    user_id=$userId -> mapRowToNote(row) or null. Ownership is baked into the
    WHERE clause, so "not found" and "owned by someone else" are
    indistinguishable — both null. Callers translate null -> notFound() (404,
    not 403) so existence isn't revealed (SPEC §8, §14.2).
  - getNotesByUser(userId): SELECT * FROM notes WHERE user_id=$userId ORDER BY
    updated_at DESC -> Note[] (rows.map(mapRowToNote)). Scoped to the owner;
    newest-edited first to drive the dashboard listing.
  - All SQL parameterized ($id/$userId binds). Reuses get()/query() from lib/db
    and mapRowToNote() from lib/notes (no new SELECT-and-map duplication).
  - VERIFIED: 14-assert throwaway Bun smoke test (DATABASE_PATH=:memory:, stub
    `user` table + alice/bob rows). Covers: owner fetch returns mapped Note
    (id/title/parsed contentJson/isPublic), AUTHZ — bob gets null for alice's
    note + missing id gets null, per-user list scoping (alice 2 / bob 1, no
    cross-leak), empty array for a user with no notes, and ORDER BY updated_at
    DESC verified deterministically via explicit distinct timestamps (createNote
    ms-resolution timestamps can tie). 14/14 pass under Bun. Smoke file removed.
    `bun run build` PASS (typechecks), `bun run lint` PASS.

[database] Implement updateNote function — passes=true
  - Appended updateNote() to lib/notes.ts (sync, per the repo-fn convention:
    reads/update/delete stay sync; driver is sync; the actions layer wraps them).
    Added `type QueryParams` to the existing `import { ... } from "./db"`.
  - Signature: updateNote(userId, noteId, data: UpdateNoteInput): Note | null
    where UpdateNoteInput = { title?: string | null; contentJson?: JSONContent }.
    `undefined` field = leave untouched; title accepts null to clear it (column
    is nullable, SPEC §5.4).
  - PARTIAL UPDATE: builds the SET clause from only the provided fields. The
    assignment fragments ("title = $title", "content_json = $contentJson") are a
    FIXED, hardcoded set — no user input is ever interpolated into SQL; every
    value is still bound via $name params (SPEC §14.3 / CLAUDE.md invariant).
    Flagging so a future iter doesn't "fix" the `${assignments.join()}` as if it
    were injection — it's column names, not user data.
  - updated_at = new Date().toISOString() is ALWAYS bumped (even on an empty
    patch) so dashboard ORDER BY updated_at DESC reflects the edit.
  - AUTHZ baked into WHERE (id=$id AND user_id=$userId): a missing or
    someone-else's note updates 0 rows; the fn then RE-READS via getNoteById
    (same ownership scope) and returns null — never revealing existence
    (404-not-403 story, SPEC §8/§14.2). Re-read also guarantees the returned
    Note mirrors the persisted row.
  - VERIFIED: 19-assert throwaway Bun smoke test (DATABASE_PATH=:memory:, stub
    `user` table + alice/bob). Covers: title-only (content untouched, updated_at
    bumped > created_at, created_at unchanged, persisted), content-only (changed,
    title kept, stored as JSON *string*), both fields, title->null clear,
    AUTHZ (bob gets null + alice's note unchanged), missing id -> null, empty
    patch (returns Note, bumps updated_at, keeps fields). 19/19 pass under Bun.
    Smoke file removed. `bun run build` PASS, `bun run lint` PASS.
  - REPO HAZARD NOTED (not introduced by this task): the PostToolUse format hook
    runs `bun run format` (oxfmt), which CANNOT parse the Next.js skeleton JSX in
    app/page.tsx and can transiently corrupt it — one `bun run build` failed on a
    mangled page.tsx, but a re-run against the on-disk (valid) file PASSED. If a
    future build fails on app/page.tsx JSX, re-run; the file itself is fine.

[database] Implement deleteNote function — passes=true
  - Appended deleteNote() to lib/notes.ts (sync, per the repo-fn convention:
    reads/update/delete stay sync; the actions layer wraps them). Reuses `run`
    from the existing lib/db import — no new imports.
  - Signature: deleteNote(userId, noteId): boolean. Single parameterized DELETE
    WHERE id=$id AND user_id=$userId; returns run().changes > 0.
  - AUTHZ baked into WHERE (mirrors updateNote): a missing or someone-else's note
    deletes 0 rows -> returns false, never revealing existence (404-not-403 story,
    SPEC §8/§14.2). Idempotent — re-deleting an already-gone note returns false,
    no throw.
  - VERIFIED: 6-assert throwaway Bun smoke test (DATABASE_PATH=:memory:, stub
    `user` table + alice/bob). Covers: non-owner delete -> false + note untouched,
    missing id -> false, owner delete -> true + note gone (getNoteById null),
    re-delete -> false. 6/6 pass. Smoke file removed.
    `bun run build` PASS (typechecks), `bun run lint` PASS (clean).

[database] Implement note sharing functions — passes=true
  - Appended setNotePublic() + getNoteByPublicSlug() to lib/notes.ts (sync, per
    the repo-fn convention). Reuses get/run from lib/db + getNoteById/
    generatePublicSlug/mapRowToNote already in lib/notes — no new imports. This
    COMPLETES the database/data-layer category.
  - setNotePublic(userId, noteId, isPublic): Note | null. Reads via getNoteById
    first (ownership baked into its WHERE), so a missing/someone-else's note ->
    null with no existence leak (SPEC §14.2). Enable: mint a slug ONLY if
    public_slug is NULL (re-share reuses the link), set is_public=1. Disable: set
    is_public=0 but KEEP the slug. updated_at bumped. Single parameterized UPDATE
    scoped to id+user_id.
  - getNoteByPublicSlug(slug): Note | null. SELECT ... WHERE public_slug=$slug
    AND is_public=1 (no userId — the public route). Gated SOLELY on is_public=1,
    looked up by the share token (never the internal id) — SPEC §10.1/§10.3/§14.5
    + CLAUDE.md invariant. An unshared note that still carries a slug 404s.
  - SPEC-vs-prd CONFLICT RESOLVED: prd step says "clear public_slug when
    disabling"; SPEC §10.2 + CLAUDE.md invariant say KEEP it (re-share yields the
    same URL; lookup gates on is_public=1 so old links 404 while unshared). SPEC
    is authoritative (CLAUDE.md) -> kept the slug. Documented in the fn docstring
    so a future iter doesn't "fix" it back to clearing.
  - VERIFIED: 18-assert throwaway Bun smoke test (DATABASE_PATH=:memory:, stub
    `user` table + alice/bob). Covers: enable mints 21-char slug + flips public +
    bumps updated_at, public lookup finds it (content maps), unknown slug -> null,
    disable keeps slug but public lookup 404s, re-enable REUSES same slug + lookup
    works again, AUTHZ (bob/missing -> null, alice's note untouched). 18/18 pass.
    Smoke file removed. `bun run build` PASS, `bun run lint` PASS (clean).

[auth] Create better-auth API route — passes=true
  - Created app/api/auth/[...all]/route.ts: the single catch-all that serves
    EVERY auth endpoint (sign-up/sign-in/sign-out/session/...). Per SPEC §7.2:
    `export const { GET, POST } = toNextJsHandler(auth)` (better-auth/next-js).
    Followed SPEC over the prd's "use auth.handler" wording — toNextJsHandler is
    the idiomatic App Router wrapper and internally uses auth.handler.
  - RISKY-INTEGRATION FIX (the real work this iter): adding the route pulled
    lib/auth.ts -> lib/db.ts into a BUILT route bundle for the first time, which
    exposed that Turbopack can't handle lib/db.ts's dynamic driver require. It
    statically rewrote `nodeRequire(specifier)` into a build-time throw stub
    ("Cannot find module as expression is too dynamic"), failing
    `next build` at "Collecting page data for /api/auth/[...all]". The
    `turbopackIgnore` magic comment did NOT help (Turbopack doesn't honor it on a
    createRequire alias). Earlier `bun run build` PASSes were illusory — nothing
    imported lib/auth/db into a route until now, so the dynamic require was never
    bundled.
  - RESOLUTION: replaced createRequire + dynamic require in lib/db.ts with
    `process.getBuiltinModule(specifier)`. It's a plain runtime method call (not
    require/import/require.resolve/new Worker), so Turbopack leaves it untouched.
    Verified BOTH runtimes load their driver: bun:sqlite under Bun
    (process.getBuiltinModule('bun:sqlite').Database), node:sqlite under Node 24
    for Vitest (.DatabaseSync). Removed the now-unused createRequire/node:module
    import. Driver-agnostic dual-runtime design preserved (CLAUDE.md/SPEC §3.2).
  - VERIFIED: `bun run build` PASS (/api/auth/[...all] registered as ƒ dynamic),
    `bun run lint` PASS, dual-runtime db smoke (Bun + Node both load + query).
    Live endpoint checks vs the dev server: GET /api/auth/ok -> {"ok":true} 200;
    GET /api/auth/get-session (no cookie) -> null 200; POST /api/auth/sign-up/
    email -> 200 with a persisted user + token (full integration through the
    shared getDb() handle, incl. the new getBuiltinModule driver path).

[auth] Implement server-side auth helpers — passes=true
  - Added getSession() + requireAuth() to lib/auth.ts (per prd step "Export
    helpers from lib/auth.ts" — NOT lib/session.ts; prd wins on file path). These
    are the authoritative server-side session surface (SPEC §7.5); the optimistic
    middleware cookie check is never the real gate.
  - getSession(): Promise<Session> — wraps auth.api.getSession({ headers: await
    headers() }) from next/headers; returns { user, session } | null. Validated
    server-side (not just cookie-presence). Exported a `Session` type alias
    (Awaited<ReturnType<typeof auth.api.getSession>>) so callers get the shape.
  - requireAuth(): Promise<NonNullable<Session>> — getSession() then redirect()s
    to "/authenticate" when null (prd's auth route is /authenticate, not SPEC's
    /login). redirect()'s `never` return narrows the result to a guaranteed
    non-null Session, so protected pages/actions can use session.user.id directly.
  - ROUTE NOTE: redirect target is /authenticate and protected dashboard is
    /dashboard — the prd's route names (app/authenticate, app/dashboard,
    app/notes/[id], app/p/[slug]) differ from SPEC §11 (/login, /notes,
    /share/[publicId]). prd is what `passes` tracks; following prd routes.
  - Imports next/headers + next/navigation at module top. lib/auth.ts is already
    server-only (top-level await migrations, better-auth/next-js), so no new
    client coupling; build confirms it still bundles into the auth route.
  - VERIFIED: 7-assert throwaway Bun smoke (DATABASE_PATH=:memory:) of the
    underlying auth.api.getSession the helpers wrap — no cookie -> null, sign-up
    issues Set-Cookie, valid cookie -> session with correct user.email/name +
    token, bogus cookie -> null (server-validated, not presence-only). 7/7 pass.
    (The helpers themselves need Next's request scope so can't be called from a
    bare Bun script; build typechecks them + the smoke proves the wrapped API.)
    Smoke file removed. `bun run build` PASS (helpers bundle into /api/auth route),
    `bun run lint` PASS.

[auth] Create auth client for frontend — passes=true
  - Created lib/auth-client.ts: the browser-side auth surface for "use client"
    components (SPEC §7.3). `export const authClient = createAuthClient()` from
    "better-auth/react" — baseURL OMITTED on purpose: app + /api/auth share one
    origin, so the client defaults to the current origin and talks to the
    catch-all handler (app/api/auth/[...all]/route.ts).
  - Per prd "Export signIn, signUp, signOut + useSession": added named re-exports
    via `export const { signIn, signUp, signOut, useSession } = authClient` for
    ergonomic call sites (signIn.email / signUp.email / signOut() / useSession()).
    Confirmed names against current better-auth/react docs (context7).
  - NO lib/db / lib/auth import here, so no Turbopack dynamic-require concern; this
    module is client-safe and bundles cleanly.
  - VERIFIED: smoke import under Bun — exports authClient, signIn, signOut, signUp,
    useSession; signIn.email is a function, useSession is a function. `bun run lint`
    PASS (clean), `bun run build` PASS (compiled + TypeScript ran + 4 static pages).
    No UI in this task -> Playwright N/A. test/test:e2e unchanged (see below).
  - UNBLOCKS auth UI: SignUpForm/LoginForm/LogoutButton can now import signUp/
    signIn/signOut/useSession from "@/lib/auth-client". Next suggested: the auth
    UI forms -> app/authenticate page -> Header + LogoutButton.

[ui] Build sign up form component — passes=true   (FIRST UI task; opens UI phase)
  - Created components/SignUpForm.tsx: "use client" controlled form (name/email/
    password via useState — minimal state, no useEffect). Submits through the
    better-auth browser client `signUp.email({ name, email, password })` from
    @/lib/auth-client (the catch-all handler persists the user). Confirmed the
    `{ data, error }` return shape + error.message via context7 (better-auth docs).
  - COMPONENT API: props `{ onSuccess?: () => void }`. Per the prd step "Handle
    success (will redirect in parent)" the form does NOT navigate itself — it
    calls onSuccess on success and the parent (app/authenticate, a later task)
    owns the redirect to /dashboard. Flagging so a future iter doesn't add a
    router.push here.
  - CLIENT-SIDE VALIDATION (fast feedback only; server stays authoritative per
    SPEC §13): name non-empty, email matches a pragmatic regex, password >= 8
    (MIN_PASSWORD_LENGTH = better-auth's emailAndPassword default). On failure
    sets an inline error and returns BEFORE calling the server. Server errors are
    surfaced from error.message into the same <p role="alert"> region.
  - A11y/modern React: semantic <form> + <label htmlFor>/id pairs, autoComplete
    (name/email/new-password), required + minLength, role="alert" error, inputs+
    button disabled while pending ("Creating account…"). noValidate so our custom
    messages show instead of native bubbles. Tailwind v4 utilities, foreground/
    background theme tokens (dark-mode aware).
  - VERIFIED with Playwright MCP against the LIVE dev server (existing instance on
    :3000 — derisks the flagged Windows Bun+Next dev-server risk; it serves fine).
    Drove a TEMP host route app/signup-smoke/page.tsx (rendered <SignUpForm
    onSuccess={() => show "SIGNUP_OK"} />), then REMOVED it (deliverable is just
    the component): (1) form renders Name/Email/Password + Sign up; (2) invalid
    email -> inline alert "Please enter a valid email address." (no server call);
    (3) valid submit -> real better-auth signup succeeds -> onSuccess fires ->
    "SIGNUP_OK" rendered; (4) 0 console errors. `bun run build` PASS (back to 3
    routes after temp removal), `bun run lint` PASS.
  - NOTE: full signup writes a user row to the dev DB data/app.sqlite (gitignored)
    — used a throwaway unique email. Harmless.
  - NEXT: components/LoginForm.tsx (same pattern, signIn.email, no name field) ->
    app/authenticate/page.tsx (toggles SignUpForm/LoginForm, redirects to
    /dashboard on success) -> Header (server, getSession) + LogoutButton.

[ui] Build login form component — passes=true
  - Created components/LoginForm.tsx: "use client" controlled form, a near-mirror
    of SignUpForm (same Tailwind styling, a11y, error region, pending handling)
    but WITHOUT the name field and signing in via signIn.email({ email, password })
    from @/lib/auth-client. Same `{ error }` return-shape handling.
  - COMPONENT API: props `{ onSuccess?: () => void }`. Per prd step "Handle success
    (will redirect in parent)" the form does NOT navigate itself — calls onSuccess;
    the parent (app/authenticate, next task) owns the /dashboard redirect. Flagging
    so a future iter doesn't add router.push here.
  - CLIENT-SIDE VALIDATION (fast feedback only; server authoritative, SPEC §13):
    email matches the same pragmatic regex as SignUpForm, password non-empty (no
    min-length on LOGIN — length is a sign-up rule; an existing account's password
    is whatever it is, and the server rejects wrong creds anyway). On failure sets
    an inline error and returns BEFORE the server call. Server errors surfaced from
    error.message into the same <p role="alert"> region.
  - A11y: semantic <form>, <label htmlFor>/id pairs, autoComplete email +
    current-password (vs SignUpForm's new-password), required, role="alert",
    inputs+button disabled while pending ("Signing in…"), noValidate.
  - VERIFIED with Playwright MCP against the LIVE dev server (:3000): drove a TEMP
    host route app/login-smoke/page.tsx (<LoginForm onSuccess={() => show
    "LOGIN_OK"} />), then REMOVED it (deliverable is just the component). Seeded a
    real user via POST /api/auth/sign-up/email first. Checks: (1) renders Email/
    Password/Log in; (2) invalid email -> inline alert "Please enter a valid email
    address." (no server call); (3) VALID creds -> real better-auth sign-in
    succeeds -> onSuccess fires -> "LOGIN_OK" rendered, 0 console errors; (4) WRONG
    password -> server error surfaced inline "Invalid email or password" (the only
    console error is the expected 401 network response, not a code bug).
    `bun run build` PASS (3 routes after temp removal), `bun run lint` PASS.
  - NEXT: app/authenticate/page.tsx (toggles SignUpForm/LoginForm, redirects to
    /dashboard on success) -> Header (server, getSession) + LogoutButton.

[ui] Create authentication page — passes=true
  - Created app/authenticate/page.tsx: "use client" page that hosts BOTH forms
    and toggles between them IN PLACE (no navigation). useState<Mode> ("login" |
    "signup"); the page owns ONLY the toggle + post-success redirect — each form
    still owns its own input state/validation/error display (SignUpForm/LoginForm
    already built). Renders LoginForm or SignUpForm conditionally; switching mode
    remounts the other form so its state (incl. password) resets — desirable.
  - REDIRECT: forms call onSuccess -> handleSuccess does router.push("/dashboard")
    + router.refresh() (next/navigation). refresh() makes server components re-read
    the session cookie that better-auth's nextCookies() set during the client auth
    call. /dashboard is a LATER prd task so it 404s for now — the redirect itself
    is correct; the 404 is expected until the dashboard page exists.
  - SCOPE NOTE: did NOT add "redirect away if already authed" (SPEC §11 mentions it
    for /login,/signup, but it's NOT a prd step here AND /dashboard doesn't exist
    yet). That belongs to the (app) layout / middleware auth-gate task. Flagging so
    a future iter doesn't think it's missing.
  - STYLE: centered card (flex min-h-screen items-center justify-center), max-w-sm
    column, mode-aware <h1> ("Welcome back" / "Create your account") + subtitle,
    bottom toggle line ("Don't have an account? Sign up" <-> "Already have an
    account? Log in") as a real <button>. Uses foreground/background theme tokens
    (text-foreground/60 opacity modifiers work — colors registered in @theme).
    Semantic <main>/<header>/<h1>, dark-mode aware.
  - VERIFIED with Playwright MCP against the LIVE dev server (:3000): (1) /authenticate
    renders in LOGIN mode (Welcome back + Email/Password/Log in); (2) clicking the
    "Sign up" toggle swaps to SIGNUP mode (Create your account + Name/Email/Password/
    Sign up + "Already have an account? Log in"); (3) filling a fresh unique email +
    submitting -> real better-auth signup succeeds -> onSuccess fires -> URL changes
    to /dashboard. The ONLY console errors are the expected /dashboard 404s (page is
    a later task), NOT a code bug. `bun run build` PASS (/authenticate registered as
    a static ○ route, 4 routes total), `bun run lint` PASS (clean).
  - NOTE: signup wrote a user row to dev DB data/app.sqlite (gitignored) via a
    throwaway unique email (ralph-authpage-0628@example.com). Harmless.
  - NEXT: Header (server component, getSession()) + LogoutButton ("use client",
    signOut) -> then the ACTIONS layer (lib/actions/notes.ts "use server") which
    unblocks the dashboard/editor/share UI.

[actions] Create server action for creating notes — passes=true
  (FIRST actions task; opens the ACTIONS layer + adds lib/validation.ts. Chose
  this foundation over the Header/LogoutButton UI tasks per the loop rule
  "foundation/risky before routine UI" — actions unblock the whole CRUD UI.)
  - Created lib/validation.ts (SPEC §13 — the shared Zod surface for all
    mutations; client is never trusted): TitleSchema (z.string().trim().max(200)),
    NoteContentSchema (z.looseObject requiring type:"doc" + content array, .refine
    caps serialized size at MAX_CONTENT_BYTES=1MB via TextEncoder byte count),
    CreateNoteSchema ({ title?, contentJson? } both optional). Used Zod v4 API
    (z.looseObject, not deprecated .passthrough()) — verified vs zod 4.4.3 via
    context7. looseObject KEEPS extra top-level doc keys (e.g. attrs) instead of
    silently dropping them. Did NOT deep-validate node types (editor whitelist in
    lib/tiptap.ts handles that, a later task) — shape + size only, defense in depth.
  - Created lib/actions/notes.ts ("use server"): exports type ActionResult<T> =
    { ok:true; data:T } | { ok:false; error:string } (serializable result so
    client callers can react to failures; will be reused by update/delete/share
    actions) + createNoteAction(input?). Guard order session -> Zod (only when
    input supplied) -> SQL (createNote repo) -> revalidatePath("/dashboard").
  - DECISION: action uses getSession() and RETURNS an error result for the
    unauthenticated case (not requireAuth()'s thrown redirect). Matches prd
    "Return created note or error" + keeps the action serializable/testable; the
    client form decides how to recover. requireAuth() stays the gate for PAGES.
    Flagging so a future iter doesn't swap it to requireAuth here.
  - With no/empty input the repo applies defaults ("Untitled note" + empty TipTap
    doc). Parsed contentJson cast to JSONContent when handed to createNote.
  - VERIFIED: `bun run build` PASS (TS typechecks both new files; build graph
    unchanged — 4 routes — since nothing imports the action yet), `bun run lint`
    PASS. 13-assert throwaway Bun smoke of lib/validation (empty ok, title trim +
    max + non-string reject, doc shape accept/reject, loose elements + extra keys
    preserved, 1MB oversize reject, both-fields) — 13/13 pass, file removed.
    The action wiring (getSession/revalidatePath) needs a Next request scope so
    it isn't bare-script testable; full e2e arrives with CreateNoteButton. No UI
    in this task -> Playwright N/A.
  - NEXT (actions, in file order): updateNoteAction (ownership + UpdateNoteSchema +
    revalidate), deleteNoteAction, toggleShareAction. Then dashboard/editor/share
    UI consume these. Reuse ActionResult<T> + add UpdateNoteSchema to lib/validation.

[actions] Create server action for updating notes — passes=true
  - Added updateNoteAction(noteId, input) to lib/actions/notes.ts and
    UpdateNoteSchema/UpdateNoteSchemaInput to lib/validation.ts. Reuses the
    repo's updateNote/getNoteById/UpdateNoteInput + the ActionResult<T> shape
    from createNoteAction (serializable result; client decides how to recover).
  - GUARD ORDER (CLAUDE.md / SPEC §12): session -> ownership -> Zod -> SQL ->
    revalidate. Explicit ownership check via getNoteById(userId,noteId) BEFORE
    validating/writing: a missing OR someone-else's note both return "Note not
    found." with no existence leak (SPEC §8/§14.2, 404-not-403). updateNote's
    WHERE also re-enforces ownership, so a concurrent delete between the check
    and the write still resolves to "not found" (handled).
  - VALIDATION: UpdateNoteSchema = { title?: TitleSchema.nullable().optional(),
    contentJson?: NoteContentSchema.optional() } — a partial patch. title may be
    null to CLEAR it (column nullable, SPEC §5.4); absent field = untouched.
    contentJson reuses NoteContentSchema (doc-shape + 1MB size cap). Only
    fields actually present in parsed.data are forwarded to updateNote, so an
    omitted title isn't coerced to null. Parsed contentJson cast to JSONContent.
  - REVALIDATE: both /dashboard (list: title/updated_at) and /notes/${noteId}
    (the editor page) so the edit shows up in both places (prd "Revalidate path
    after update").
  - DECISION: action uses getSession() and RETURNS an error result for the
    unauthenticated/not-found cases (not requireAuth()'s thrown redirect) —
    matches prd "Return updated note or error" + keeps the action serializable/
    testable. Consistent with createNoteAction. requireAuth() stays the PAGE gate.
  - VERIFIED: `bun run build` PASS (TS typechecks both files; graph unchanged at
    4 routes — nothing imports the action yet), `bun run lint` PASS (clean).
    13-assert throwaway Bun smoke of UpdateNoteSchema (empty patch ok, title
    string/trim/null-clear/non-string-reject/over-max-reject, contentJson
    accept + missing-type/non-doc/oversize reject, both-fields) — 13/13 pass,
    file removed. The action wiring (getSession/revalidatePath) needs a Next
    request scope so isn't bare-script testable; full e2e arrives with the editor
    Save flow. underlying updateNote repo already has a 19-assert smoke from its
    own iter. No UI in this task -> Playwright N/A.
  - NEXT (actions, in file order): deleteNoteAction (ownership + revalidate
    /dashboard), toggleShareAction (ownership + setNotePublic + revalidate note
    page). Then dashboard/editor/share UI consume create/update/delete/share.

[actions] Create server action for deleting notes — passes=true
  - Added deleteNoteAction(noteId) to lib/actions/notes.ts and `deleteNote` to the
    existing import from @/lib/notes. Reuses the ActionResult<T> shape from the
    create/update actions (serializable result; client decides how to recover).
  - Signature: deleteNoteAction(noteId): Promise<ActionResult<{ id: string }>>.
    Returns the deleted id on success (useful for the editor's post-delete
    redirect to /dashboard); an error result otherwise.
  - GUARD ORDER (CLAUDE.md / SPEC §12): session -> ownership+SQL (ATOMIC) ->
    revalidate. UNLIKE updateNoteAction (which does an explicit getNoteById check
    before validating/writing because it has a Zod step in between), delete has
    NOTHING to validate between check and write — so it calls deleteNote() ONCE
    and branches on its boolean. deleteNote enforces ownership in its WHERE and
    reports whether a row was removed, so check+delete are a single atomic step
    (no TOCTOU window). Flagging so a future iter doesn't "add" a redundant
    getNoteById ownership check and reintroduce a race.
  - AUTHZ: a missing OR someone-else's note removes 0 rows -> false -> "Note not
    found.", never revealing whether the id exists (SPEC §8/§14.2, 404-not-403).
    Idempotent: re-deleting an already-gone note returns the same "not found".
  - DECISION: uses getSession() and RETURNS an error result for the
    unauthenticated case (not requireAuth()'s thrown redirect) — matches prd
    "Return success/error result" + keeps the action serializable/testable.
    Consistent with create/update actions. requireAuth() stays the PAGE gate.
  - REVALIDATE: both /dashboard (the list loses the row) and /notes/${noteId}
    (the now-gone editor page) so stale caches are purged after deletion. prd
    only asks for the dashboard path; added the note path for cache correctness,
    consistent with updateNoteAction.
  - VERIFIED: `bun run build` PASS (TS typechecks; graph unchanged at 3 routes —
    nothing imports the action yet), `bun run lint` PASS (clean). 6-assert
    throwaway Bun smoke of the deleteNote repo path the action wraps
    (DATABASE_PATH=:memory:, stub `user` + alice/bob): non-owner delete -> false +
    note intact, missing id -> false, owner delete -> true + note gone, re-delete
    -> false. 6/6 pass, file removed. The action wiring (getSession/
    revalidatePath) needs a Next request scope so isn't bare-script testable; full
    e2e arrives with the editor Delete flow. No UI in this task -> Playwright N/A.
  - NEXT (last actions task): toggleShareAction(noteId, isPublic) — ownership +
    setNotePublic + revalidate note page; returns updated note with publicSlug.
    Then dashboard/editor/share UI consume create/update/delete/share.

[actions] Create server action for toggling note sharing — passes=true
  (COMPLETES the actions category — all 4 action tasks now passes=true.)
  - Added toggleShareAction(noteId, isPublic) to lib/actions/notes.ts and
    `setNotePublic` to the existing import from @/lib/notes. Reuses the
    ActionResult<T> shape from the create/update/delete actions.
  - Signature: toggleShareAction(noteId, isPublic): Promise<ActionResult<Note>>.
    Returns the updated Note (incl. publicSlug) on success (prd "Return updated
    note with publicSlug"); an error result otherwise.
  - GUARD ORDER (CLAUDE.md / SPEC §12): session -> validate flag -> ownership+SQL
    (ATOMIC) -> revalidate. Like deleteNoteAction, setNotePublic enforces
    ownership in its own owner-scoped read/write and returns null for a missing
    OR someone-else's note, so the ownership check and the toggle are a SINGLE
    atomic step (no TOCTOU window — nothing to validate between a separate check
    and the write). Flagging so a future iter doesn't add a redundant getNoteById
    ownership check and reintroduce a race.
  - VALIDATION: the only input besides noteId is the boolean flag, so instead of
    a Zod schema there's an inline `typeof isPublic !== "boolean"` guard (don't
    trust the client's runtime type). No note title/content to validate here.
  - AUTHZ: missing/non-owned note -> setNotePublic null -> "Note not found.",
    never revealing whether the id exists (SPEC §8/§14.2, 404-not-403).
  - SHARE SEMANTICS (delegated to setNotePublic, already smoked): enable mints a
    public_slug only if none exists (re-share reuses the link); disable flips
    is_public=0 but KEEPS the slug (SPEC §10.2 — old links 404 via the
    is_public=1 gate, re-share yields the same URL). Confirmed by this iter's
    smoke (see below).
  - REVALIDATE: /dashboard (share status in list) + /notes/${noteId} (the share
    controls) + /p/${publicSlug} when a slug exists (so the public page cache
    reflects the new visibility — newly shared appears, unshared 404s). prd asks
    only "Revalidate note page"; added the others for cache correctness,
    consistent with update/deleteNoteAction.
  - DECISION: uses getSession() and RETURNS an error result for the
    unauthenticated case (not requireAuth()'s thrown redirect) — keeps the action
    serializable/testable. Consistent with the other 3 actions.
  - VERIFIED: `bun run build` PASS (TS typechecks; graph unchanged at 4 routes —
    nothing imports the action yet; routes: /, /_not-found, /api/auth/[...all],
    /authenticate), `bun run lint` PASS (clean). 10-assert throwaway Bun smoke of
    the setNotePublic path the action wraps (DATABASE_PATH=:memory:, stub `user` +
    alice/bob): new note private, enable mints 21-char slug + public lookup finds
    it, disable keeps slug but lookup 404s, re-enable reuses same slug, AUTHZ
    (bob/missing -> null). 10/10 pass, file removed. The action wiring (getSession/
    revalidatePath) needs a Next request scope so isn't bare-script testable; full
    e2e arrives with the ShareToggle UI. No UI in this task -> Playwright N/A.
  - NEXT: ACTIONS CATEGORY COMPLETE. Frontier is now the CRUD/editor/share UI
    that consumes these actions: Header + LogoutButton (auth UI), then dashboard
    (page + NoteList + CreateNoteButton + loading/error), the /notes/[id] editor
    (TipTap — needs lib/tiptap.ts, @tiptap/react already installed) with save/
    delete/share, the /p/[slug] public viewer (@tiptap/static-renderer — NOT yet
    installed), landing/styling/responsive, then Vitest + Playwright test setup.

[ui] Implement logout functionality — passes=true
  - Created components/LogoutButton.tsx: "use client" leaf component. Built it
    FIRST (leaf-first) because the Header task depends on it ("Show ... Logout
    button"); Header consumes this component next iteration. Mirrors the existing
    auth-UI conventions (pending state via useState, useRouter from next/
    navigation, foreground/background theme tokens, rounded-md/text-sm styling).
  - BEHAVIOR: onClick -> signOut() (from @/lib/auth-client) -> router.push("/") +
    router.refresh(). signOut clears the session cookie (better-auth nextCookies
    plugin); refresh() makes server components re-read the cleared cookie. Button
    disables + shows "Logging out…" while pending. Redirect target is "/" per prd
    ("Redirect to / after logout") — NOT /authenticate.
  - DEFERRED sub-step "Add to Header component": Header doesn't exist yet (separate
    prd task, still passes=false). The COMPONENT is the deliverable here and is
    complete + verified standalone; the Header task will import it. Flagging so a
    future iter doesn't think LogoutButton is unfinished — same pattern as the
    forms' "redirect in parent" deferral.
  - VERIFIED with Playwright MCP against the LIVE dev server (:3000) via a TEMP
    host route app/logout-smoke/page.tsx (showed live useSession() status +
    rendered <LogoutButton/>), then REMOVED it (deliverable is just the component).
    Flow: signed up a fresh user (ralph-logout-0628@example.com) -> /logout-smoke
    showed "signed-in:ralph-logout-0628@example.com" -> clicked "Log out" ->
    redirected to "/" (URL became /, 0 console errors) -> re-navigated to
    /logout-smoke -> "signed-out" (cookie cleared, session gone). All 3 prd-
    testable steps confirmed (signOut called, redirect to /, session cleared).
  - NOTE: the smoke signup wrote a user row to dev DB data/app.sqlite (gitignored)
    via a throwaway unique email. Harmless.
  - `bun run build` PASS (4 routes after temp removal — component not yet imported
    into any route so graph unchanged), `bun run lint` PASS (clean).
  - NEXT: Header (server component, getSession()) — show app name/home link,
    Login/Sign up when logged out, Dashboard link + <LogoutButton/> when logged
    in; add to app/layout.tsx. Then dashboard page + NoteList + CreateNoteButton.

[ui] Add app header with auth state — passes=true
  - components/Header.tsx (async Server Component) + wired into app/layout.tsx so
    it renders once above EVERY page. Reads the authoritative session via
    getSession() (lib/auth, SPEC §7.5) so first paint matches reality (no client
    flash). App name "Notes" always links to "/".
  - SIGNED-OUT: "Log in" + "Sign up" links, both -> /authenticate (the prd's single
    auth page that toggles login/signup; SPEC's split /login,/signup don't exist).
  - SIGNED-IN: "Dashboard" link (-> /dashboard) + <LogoutButton/> (the existing
    "use client" leaf from the logout task; Header just composes it). This SATISFIES
    the deferred "Add to Header" sub-step from the LogoutButton iter.
  - Header itself is a Server Component (no "use client"); only the LogoutButton
    island is client. Turbopack note holds: Header imports lib/auth -> lib/db, so it
    pulls the data layer into the layout bundle — builds clean via getBuiltinModule
    (don't revert to dynamic require).
  - STYLE: max-w-5xl nav, foreground/background theme tokens, dark-mode-aware
    hover/borders, responsive gap/padding (sm:). Tailwind v4 utilities only.
  - VERIFIED with Playwright MCP against the live dev server (:3000): (1) signed-out
    "/" -> Notes/Log in/Sign up, 0 console errors; (2) signed up a fresh user
    (ralph-header-0628@example.com) -> redirected to /dashboard (404, later task) ->
    Header now shows Notes/Dashboard/Log out; (3) clicked Log out -> redirect to "/"
    -> Header reverted to Log in/Sign up. The ONLY console error throughout was the
    expected /dashboard 404 (page not built yet); the 2 warnings are the default
    skeleton landing SVGs (next.svg/vercel.svg), not Header. `bun run build` PASS
    (4 routes), `bun run lint` PASS (clean).
  - BUILD HAZARD (env, not code): a first `bun run build` failed with
    SQLITE_BUSY_RECOVERY "database is locked" during page-data collection (15
    parallel workers each cold-starting auth migrations contend on the WAL). A
    plain re-run PASSED. If a future build fails on a DB lock, just re-run — it's
    transient WAL contention, not a regression.
  - NOTE: signup wrote a user row to dev DB data/app.sqlite (gitignored), throwaway
    unique email. Harmless.
  - NEXT: the dashboard cluster — app/dashboard/page.tsx (RSC, requireAuth +
    getNotesByUser) + components/NoteList.tsx + components/CreateNoteButton.tsx +
    dashboard loading/error states. Then the /notes/[id] editor (TipTap, needs
    lib/tiptap.ts) and /p/[slug] public viewer, then the test setup tasks.

[ui] Build dashboard page with RSC data fetching — passes=true
  (FIRST dashboard-cluster task; the protected /dashboard route, previously a 404
  that every prior iter's signup smoke hit. Chose it over the routine NoteList/
  CreateNoteButton presentation tasks per the loop rule "foundation/risky before
  routine": first time requireAuth() runs in a real protected-PAGE request scope,
  first getNotesByUser() in an RSC, and first pull of lib/notes -> lib/db into a
  PAGE bundle — the Turbopack/getBuiltinModule concern. It builds clean.)
  - Created app/dashboard/page.tsx: async Server Component (SPEC §3.1 reads run in
    Server Components — no REST API). Guard: `const session = await requireAuth()`
    (authoritative server-side check; redirects to /authenticate when signed out,
    its `never` return narrows session to non-null). Then `getNotesByUser(
    session.user.id)` — scoped to the owner, ORDER BY updated_at DESC.
  - DISPLAY: renders the list INLINE in the page (server-rendered <ul> of <li>
    cards, each a <Link href={`/notes/${id}`}>) with title (falls back to
    "Untitled note" on null/blank), "Updated <date>" (formatUpdatedAt: stable
    en-GB d-MMM-yyyy; safe in an RSC — output is serialized, not re-run client-
    side, so no hydration mismatch), and a Public/Private badge. EMPTY STATE: a
    dashed-border card ("No notes yet" + hint) shown when notes.length === 0;
    header subtitle switches between "You don't have any notes yet." and "N notes".
  - SCOPE/prd-wording NOTE: prd step says "Pass notes to client component for
    display". Rendered the list server-side instead (read-only links need no client
    JS; SPEC §3.1 favors RSC reads). The NEXT prd task "Create notes list component"
    (components/NoteList.tsx) will EXTRACT this inline markup into a component and
    the page will import it — that's the intended page-first-then-extract flow, not
    a missing piece. Flagging so a future iter doesn't think the page is unfinished.
    CreateNoteButton is also a separate later task, so no create button here yet.
  - STYLE: max-w-5xl main, responsive grid (sm:2 / lg:3 cols), foreground/background
    theme tokens, dark-mode-aware borders/hover, line-clamp-2 titles. Tailwind v4.
  - VERIFIED with Playwright MCP against the live dev server (:3000), 4 checks:
    (1) AUTH GATE — logged-out /dashboard redirects to /authenticate (protection
    works); (2) signed up a fresh user (ralph-dashboard-0628@example.com) -> landed
    on /dashboard showing the EMPTY STATE ("No notes yet") + Header flipped to
    Dashboard/Log out; (3) seeded 2 notes for that user via a throwaway bun script
    (_seed-dashboard.ts, REMOVED after — used createNote x2 + setNotePublic on one,
    writing to the SAME gitignored dev DB) -> reloaded -> POPULATED list: "2 notes",
    both cards with correct titles, "Updated 28 Jun 2026", Public + Private badges,
    correct /notes/<uuid> links, and correct updated_at DESC order (the just-shared
    "Project ideas" sorts above "Shopping list"); (4) 0 console errors / 0 warnings.
  - `bun run build` PASS — /dashboard now registered as ƒ (Dynamic) server-rendered
    on demand (correct: it reads cookies + queries the DB); 5 routes total. Pulling
    lib/notes -> lib/db into a page bundle built clean (getBuiltinModule driver, as
    expected — don't revert to a dynamic require). `bun run lint` PASS (clean).
  - NOTE: signup + seed wrote user/note rows to dev DB data/app.sqlite (gitignored),
    throwaway unique email. Harmless.
  - NEXT (dashboard cluster, in order): components/NoteList.tsx (extract the inline
    card list above into a reusable component, import it into the dashboard page) ->
    components/CreateNoteButton.tsx ("use client", calls createNoteAction then
    router.push(`/notes/${id}`)) -> app/dashboard/loading.tsx + error.tsx. Then the
    /notes/[id] editor (TipTap — needs lib/tiptap.ts, @tiptap/react installed) and
    /p/[slug] public viewer (@tiptap/static-renderer + @tailwindcss/typography are
    installed), then the Vitest/Playwright test-setup tasks.

[ui] Create notes list component — passes=true
  - Created components/NoteList.tsx by EXTRACTING the inline card list that the
    dashboard page already rendered into a reusable presentational component, then
    rewired app/dashboard/page.tsx to `<NoteList notes={notes} />`. This is the
    page-first-then-extract flow the dashboard iter flagged as intended — not new
    behavior, so the rendered output is byte-identical to the already-verified
    dashboard. Picked it as the clean next dependency step (data + actions layers
    complete; dashboard page done).
  - COMPONENT: Server Component (no "use client" — read-only <Link> cards, no
    client JS). Props `{ notes: Note[] }` (Note type from @/lib/notes). Owns BOTH
    the populated list AND the empty state ("No notes yet" dashed card) per the prd
    step "Show helpful message when list is empty". The page still owns the data
    fetch (requireAuth + getNotesByUser, scoped to owner) and the header/subtitle
    ("N notes" vs "no notes yet") — NoteList is pure presentation.
  - Each card: title (falls back to "Untitled note" on null/blank), "Updated
    <date>" via formatUpdatedAt (stable en-GB d-MMM-yyyy — safe in an RSC, output
    is serialized not re-run client-side), Public/Private badge, and a
    <Link href={`/notes/${id}`}>. Card grid layout (sm:2 / lg:3 cols), foreground/
    background theme tokens, dark-mode borders/hover, line-clamp-2 titles —
    Tailwind v4, unchanged from the inline markup.
  - VERIFIED with Playwright MCP against the live dev server (:3000): /dashboard
    (existing session) rendered "2 notes" + two cards from NoteList — "Project
    ideas" (Public badge) and "Shopping list" (Private badge), correct
    /notes/<uuid> links, "Updated 28 Jun 2026", and updated_at DESC order
    (shared "Project ideas" sorts first). 0 console errors / 0 warnings (only the
    React DevTools info + HMR-connected log). Empty-state branch is the same markup
    already verified in the dashboard iter. `bun run build` PASS (5 routes,
    /dashboard still ƒ Dynamic), `bun run lint` PASS (clean).
  - NEXT (dashboard cluster): components/CreateNoteButton.tsx ("use client", calls
    createNoteAction then router.push(`/notes/${id}`), add to dashboard) ->
    app/dashboard/loading.tsx + error.tsx. Then the /notes/[id] editor (TipTap —
    needs lib/tiptap.ts; @tiptap/react installed) + /p/[slug] public viewer, then
    the Vitest/Playwright test-setup tasks.

[ui] Add create note button to dashboard — passes=true
  - Created components/CreateNoteButton.tsx ("use client") and wired it into the
    app/dashboard/page.tsx header (right of the "Your notes"/count column). This is
    the next dashboard-cluster dependency the NoteList iter flagged. The data +
    actions layers are complete, so this is straightforward UI consuming an existing
    action — no new risky integration.
  - BEHAVIOR: onClick -> createNoteAction() (no input -> repo applies defaults
    "Untitled note" + empty TipTap doc) -> on ok, router.push(`/notes/${id}`) +
    router.refresh(); on error surfaces result.error in a role="alert" and re-enables.
    Keeps `pending` true THROUGH the navigation so the button stays "Creating…"/
    disabled until the editor page takes over (no flash back to idle).
  - DECISION: consumes the action's ActionResult (getSession-based, returns an error
    result for the unauthenticated case) rather than throwing — consistent with the
    auth-UI forms. The button is a client island; the auth check + INSERT stay
    server-side in createNoteAction. Mirrors the LogoutButton conventions (useState
    pending, useRouter, foreground/background theme tokens). Used the inverted
    bg-foreground/text-background style to read as the primary CTA vs the outlined
    Log out button.
  - KNOWN-OK: clicking navigates to /notes/[id], which 404s for now — the editor
    page is a later prd task ("Build note editor page with RSC", passes=false). The
    navigation itself is correct; the 404 is expected until that page exists. Same
    deferral pattern as the dashboard/Header links before their targets were built.
    Flagging so a future iter doesn't think CreateNoteButton is broken.
  - VERIFIED with Playwright MCP against the live dev server (:3000), existing
    session: /dashboard showed "2 notes" + the "New note" button in the header.
    Clicked it -> URL changed to /notes/<new-uuid> (e2adfa59-…), proving
    createNoteAction ran + the post-create navigation works. The ONLY console error
    is the expected 404 for that not-yet-built editor route (confirmed via
    console_messages — it's the /notes/<uuid> resource 404, not a code bug).
    Navigated back to /dashboard -> "3 notes", and the new note renders at the TOP
    (updated_at DESC) as "Untitled note" / Private with the SAME uuid in its
    /notes/<uuid> link — confirms the row actually persisted with the repo defaults.
  - NOTE: the click wrote a 3rd note row to the dev DB data/app.sqlite (gitignored).
    Harmless throwaway.
  - `bun run build` PASS (5 routes, /dashboard still ƒ Dynamic; CreateNoteButton
    bundles into the dashboard page as a client component), `bun run lint` PASS.
  - NEXT (dashboard cluster, last task): app/dashboard/loading.tsx (skeleton) +
    app/dashboard/error.tsx (error boundary + "Try again"). Then the /notes/[id]
    editor cluster (TipTap — needs lib/tiptap.ts; @tiptap/react installed) which
    will make the CreateNoteButton's navigation target real. Then /p/[slug] public
    viewer + landing/styling/responsive, then the Vitest/Playwright test-setup tasks.

[ui] Create loading and error states for dashboard — passes=true
  (COMPLETES the dashboard cluster. Chose this clean, self-contained, no-new-dep
  task over the riskier TipTap editor cluster — it finishes /dashboard before the
  frontier moves to /notes/[id]. Two segment files, no new routes.)
  - app/dashboard/loading.tsx: Server Component skeleton shown via Suspense while
    the dynamic, DB-backed dashboard awaits requireAuth() + getNotesByUser(). Mirrors
    page.tsx/NoteList layout EXACTLY (mx-auto max-w-5xl px-4 py-10 sm:px-6 header +
    grid gap-3 sm:grid-cols-2 lg:grid-cols-3) so there's no layout shift on load:
    header has title/subtitle/"New note"-button placeholders; 6 card skeletons each
    with a title bar + footer (date + badge) placeholder. animate-pulse on black/[.0x]
    (dark white/[.0x]) blocks; aria-busy="true" + an sr-only "Loading your notes…".
  - app/dashboard/error.tsx: the segment error boundary. MUST be "use client" and
    receive { error, reset } (Next App Router contract). Renders a red-bordered alert
    card (role="alert") — "Something went wrong" + generic "We couldn't load your
    notes. Please try again." (no internals leaked) — with a "Try again" <button> that
    calls reset() to re-render the segment. Logs the real error to console via
    useEffect for diagnostics. Same max-w-5xl shell + foreground/background theme
    tokens, dark-mode aware. (apos-escaped "couldn't" for the lint/react rule.)
  - SCOPE: loading/error are Next.js SEGMENT files (loading.tsx/error.tsx), not
    routes — they don't change the route count; build stays at 5 routes. They wrap
    the EXISTING dashboard page; no change to page.tsx/NoteList/CreateNoteButton.
  - VERIFIED with Playwright MCP against the live dev server (:3000): (1) REGRESSION —
    /dashboard (existing session) still renders fine with the new sibling files: "3
    notes" + all cards/links intact, 0 console errors. (2) BOTH transient states (hard
    to catch live) rendered via a TEMP client host route app/states-smoke/page.tsx
    that imported + rendered <DashboardLoading/> and <DashboardError error reset/>,
    screenshotted (states-smoke.png), then REMOVED the route + png. Screenshot
    confirmed: loading = header skeleton + 6 pulsing card skeletons in the 3-col grid;
    error = red alert card with heading + message + black "Try again" button. The ONLY
    console "issue" was the intentional console.error(error) from the boundary's
    useEffect — expected diagnostic logging, not a bug. Reset wired to a noop logging
    "RESET_CLICKED" in the smoke (real reset() re-renders the segment).
  - `bun run build` PASS (5 routes, /dashboard still ƒ Dynamic; rebuilt clean after
    temp-route removal), `bun run lint` PASS (clean). test/test:e2e unchanged (see
    CHECKS STATUS — still the pre-existing "no test files" / no-script state).
  - NEXT: the dashboard cluster is DONE. Frontier is the /notes/[id] editor cluster
    (the CreateNoteButton already navigates there → currently 404s): build
    lib/tiptap.ts (shared extension config — SINGLE SOURCE for editor + static
    renderer, disable non-whitelisted StarterKit nodes per SPEC §9.1) →
    app/notes/[id]/page.tsx (RSC: requireAuth + getNoteById → notFound() on
    missing/non-owned) → components/NoteEditor.tsx ("use client", @tiptap/react,
    immediatelyRender:false) → toolbar → title input → save (updateNoteAction,
    debounced) → delete → share (ShareToggle + toggleShareAction). Then /p/[slug]
    public viewer (@tiptap/static-renderer + @tailwindcss/typography both installed),
    landing/styling/responsive, then Vitest/Playwright test-setup tasks.

[ui] Build note editor page with RSC — passes=true
  (FIRST /notes/[id] editor-cluster task; makes the route REAL — it was a 404 that
  CreateNoteButton already navigates to. Chose the RSC SHELL first: it's the
  foundation/dependency root the whole editor cluster hangs off, and the riskier
  TipTap client integration is the NEXT task. Same page-first flow the dashboard
  used before <NoteList> was extracted.)
  - Created app/notes/[id]/page.tsx: async Server Component shell (SPEC §3.1/§11 —
    Server Component shell + client editor). Next 16 async params: `params:
    Promise<{ id: string }>`, awaited. GUARD ORDER (SPEC §7.5/§8/§14.2):
    requireAuth() (authoritative server-side session; redirects to /authenticate
    when signed out, `never` return narrows session non-null) -> getNoteById(
    session.user.id, id) (ownership baked into its WHERE — missing vs not-owned are
    indistinguishable, both null) -> notFound() on null = 404 NOT 403, so existence
    is never revealed.
  - RENDER: minimal read-only shell around the fetched note — "← Back to dashboard"
    link, title (falls back to "Untitled note"), "Updated <date>" (formatUpdatedAt,
    same stable en-GB label as NoteList — RSC-safe, output serialized), Public/
    Private badge (same styling tokens as NoteList), and a dashed "Editor coming
    soon" mount-point region.
  - SCOPE/prd-wording NOTE: prd step "Pass note data to editor component" — the
    TipTap editor (components/NoteEditor.tsx) is the NEXT prd task (passes=false),
    so this iter renders the shell inline and marks where the editor mounts; the
    NoteEditor task replaces that region and receives `note` (incl. note.contentJson)
    as initial data. Page-first-then-replace, exactly like dashboard->NoteList.
    Flagging so a future iter doesn't think the page is unfinished. DEPS already
    installed: @tiptap/react + @tiptap/pm + @tiptap/starter-kit + @tailwindcss/
    typography are all in package.json — the editor task needs no new installs,
    only lib/tiptap.ts (shared extension config, SINGLE SOURCE editor+renderer).
  - PARTIALLY SATISFIES later editor sub-steps already (don't redo): "Add 'Back to
    Dashboard' link" + "Handle 404 errors gracefully" land here as a side effect of
    the shell, though those belong to the "navigation and error handling" task.
  - VERIFIED with Playwright MCP against the live dev server (:3000), existing
    session (3 seeded notes): (1) OWNED note — clicked "Project ideas" on /dashboard
    -> /notes/<uuid> rendered the shell: title "Project ideas", "Updated 28 Jun
    2026", Public badge, back link, editor mount point; (2) 404 — navigated to
    /notes/00000000-0000-0000-0000-000000000000 (non-existent ~ also the not-owned
    case) -> page title "404: This page could not be found.", the ONLY console error
    is the expected 404 resource response (confirmed via console_messages), not a
    code bug; (3) auth gate is the SAME requireAuth() already verified on /dashboard
    (redirects logged-out -> /authenticate). `bun run build` PASS — /notes/[id] now
    registered as ƒ (Dynamic), 6 routes total; pulling lib/notes->lib/db into the
    page bundle built clean (getBuiltinModule driver — don't revert to a dynamic
    require). `bun run lint` PASS (clean).
  - NEXT (editor cluster, in dependency order): lib/tiptap.ts (shared `extensions`
    — disable non-whitelisted StarterKit nodes per SPEC §9.1, SINGLE SOURCE for
    editor + static renderer) -> components/NoteEditor.tsx ("use client",
    @tiptap/react useEditor/EditorContent, immediatelyRender:false, initial content
    = note.contentJson, onUpdate callback) -> wire it into this page (replace the
    placeholder) -> title input -> EditorToolbar (bold/italic/H1-3/paragraph) ->
    code+list buttons -> save (updateNoteAction, debounced auto-save) -> delete
    (confirm + deleteNoteAction -> /dashboard) -> nav/error/toasts + loading.tsx ->
    ShareToggle (toggleShareAction) + copy URL. Then /p/[slug] public viewer
    (@tiptap/static-renderer + @tailwindcss/typography installed), landing/styling/
    responsive, then Vitest/Playwright test-setup tasks.

[ui] Create TipTap editor component — passes=true   (FIRST editor-cluster build;
  the risky integration that unblocks the whole editor — chosen over the routine
  title/toolbar/save tasks per the loop rule "risky integrations before routine".)
  - Created lib/tiptap.ts: the SINGLE SOURCE OF TRUTH `extensions` array (CLAUDE.md
    invariant / SPEC §9.1). One `StarterKit.configure({...})`: heading levels [1,2,3];
    DISABLED the non-whitelisted nodes/marks (orderedList, blockquote, strike, link,
    underline) so stored docs can only contain our toolbar whitelist — also the XSS/
    schema-lockdown measure (SPEC §14.1). KEPT (StarterKit defaults): bold, italic,
    heading, paragraph, code, codeBlock, bulletList+listItem, horizontalRule + the
    structural nodes. NO "use client" / no server-only imports — this module must
    also load inside the server static renderer for /p/[slug] (later task). Used the
    NAMED export `import { StarterKit }` (the pkg exports named + default + the
    StarterKitOptions type; v3 added link/underline to the kit, hence disabling them).
  - Created components/NoteEditor.tsx ("use client"): useEditor + EditorContent from
    @tiptap/react, `extensions` from lib/tiptap, content = note.contentJson (initial
    data), immediatelyRender:false (REQUIRED under SSR/Next, SPEC §9.2 — useEditor
    returns null on first SSR render, guarded with `if (!editor) return null`).
    editorProps class = "prose dark:prose-invert max-w-none min-h-[60vh]
    focus:outline-none". onUpdate tracks the latest doc into a useRef (the save task
    reads it; ref avoids re-render per keystroke) AND calls an optional onChange prop
    — the prd's "onChange callback to track content changes".
  - COMPONENT API decision: takes the WHOLE `note: Note` (a plain serializable
    object — contentJson is parsed JSON), not just initialContent, because the later
    title/save/delete/share tasks all extend THIS component and need note.id/title/
    isPublic/publicSlug. The Server page can mount <NoteEditor note={note} /> directly
    (no onChange needed across the RSC boundary). `import type { Note }` is erased at
    compile so lib/notes -> lib/db never enters the CLIENT bundle (no Turbopack
    dynamic-require concern). Flagging so a future iter doesn't switch it to
    initialContent or add a client wrapper.
  - Enabled the typography plugin: added `@plugin "@tailwindcss/typography";` to
    app/globals.css (FIRST use of `prose`; @tailwindcss/typography already installed).
    Builds clean; the editor element's computed class confirms prose applied.
  - Wired into app/notes/[id]/page.tsx: replaced the "Editor coming soon" dashed
    placeholder with <NoteEditor note={note} /> (page-first-then-mount flow). The
    page's own auth/ownership/404 guard is unchanged.
  - VERIFIED with Playwright MCP against the live dev server (:3000), already-authed
    session: opened an existing note (/notes/<uuid>) -> the TipTap editor mounted
    (div.tiptap.ProseMirror, contenteditable=true) initialised from the note's stored
    content (its single empty paragraph). Clicked in + typed "Hello from the TipTap
    editor" -> text rendered live in the editable surface (onUpdate fired). Computed
    className = "tiptap ProseMirror prose dark:prose-invert max-w-none min-h-[60vh]
    focus:outline-none ProseMirror-focused" (editorProps + prose confirmed). 0 console
    errors/warnings across the flow. NOTE: no Save wired yet (later task), so the typed
    edit did NOT persist to the DB — the note row is unchanged. dev DB is gitignored.
  - `bun run build` PASS (6 routes; /notes/[id] now bundles the client editor + its
    StarterKit deps — compiled clean), `bun run lint` PASS (clean). test/test:e2e
    unchanged (see CHECKS STATUS — no suites yet, later prd tasks author them).
  - NEXT (editor cluster, in order): title input in NoteEditor (controlled, init from
    note.title) -> components/EditorToolbar.tsx (bold/italic/H1-3/paragraph, active
    state via editor.isActive) -> code+inline-code+bullet-list+HR buttons -> save
    (updateNoteAction, debounced auto-save, status indicator, skip initial load) ->
    delete (confirm -> deleteNoteAction -> /dashboard) -> nav/error/toasts +
    loading.tsx -> ShareToggle (toggleShareAction) + copy URL -> /p/[slug] public
    viewer (@tiptap/static-renderer — NOT installed yet; reuse lib/tiptap `extensions`)
    -> landing/styling/responsive -> Vitest + Playwright test setup.

[ui] Add title input to editor — passes=true
  - Extended components/NoteEditor.tsx with a CONTROLLED title input (the prd
    deliverable). Added `const [title, setTitle] = useState(note.title ?? "")`
    (null -> "" keeps the input controlled + shows the placeholder), a controlled
    <input value={title} onChange={...}> at the TOP of the editor card (above the
    EditorContent / future toolbar mount point), and an OPTIONAL onTitleChange prop
    (parallel to the existing onChange) so the later Save task can read the latest
    title. State (not a ref) per the prd step "Track title changes in state".
  - maxLength={200} mirrors TitleSchema's cap (lib/validation.ts, SPEC §13) so the
    input can't produce a title updateNoteAction would reject. placeholder
    "Untitled note", aria-label "Note title" (a11y — the input is the title surface,
    not a heading). Style: w-full bg-transparent text-2xl font-semibold tracking-
    tight placeholder:text-foreground/40 focus:outline-none (large borderless
    Notion-style title field; foreground theme token, dark-mode aware).
  - PAGE CHANGE (de-duplicate): app/notes/[id]/page.tsx previously rendered the
    title as a static <h1> in its header. With the editable input now serving as the
    title, removed that <h1> to avoid two stacked titles; KEPT the metadata row
    (Updated <date> + Public/Private badge), now right-aligned (justify-end). The
    page's auth/ownership/404 guard + back link are unchanged. Flagging so a future
    iter doesn't think the page lost its heading — the title lives in the input now.
  - SCOPE: the title is tracked in state but NOT YET PERSISTED — wiring it to
    updateNoteAction (debounced auto-save / Save button) is the later "Implement
    save functionality" task, which reads this title state + the contentRef. onChange
    + onTitleChange callbacks are the hooks it will use. Flagging so a future iter
    doesn't add a save call here.
  - VERIFIED with Playwright MCP against the live dev server (:3000), existing
    session (3 seeded notes): opened "Project ideas" (/notes/<uuid>) -> the title
    renders as a textbox aria-label "Note title" INITIALISED with "Project ideas"
    (init-from-note.title confirmed), placeholder "Untitled note", maxLength=200
    (matches TitleSchema), and NO duplicate <h1> (only the metadata row Updated 28
    Jun 2026 + Public). CONTROLLED check: Ctrl+A then typed "My edited title" slowly
    (per-keystroke onChange) -> input value became "My edited title" (state drives
    the value). 0 console errors/warnings. The typed edit did NOT persist (no save
    wired yet) -> note row unchanged; dev DB gitignored.
  - `bun run build` PASS (6 routes; /notes/[id] still ƒ Dynamic, NoteEditor bundles
    the input cleanly), `bun run lint` PASS (clean). test/test:e2e unchanged (see
    CHECKS STATUS).
  - NEXT (editor cluster, in order): components/EditorToolbar.tsx (bold/italic/
    H1-3/paragraph, active state via editor.isActive) -> code+inline-code+bullet-
    list+HR buttons -> save (updateNoteAction, debounced auto-save, status
    indicator, skip initial load — reads the title state + contentRef from this
    component) -> delete (confirm -> deleteNoteAction -> /dashboard) -> nav/error/
    toasts + loading.tsx -> ShareToggle (toggleShareAction) + copy URL. Then
    /p/[slug] public viewer, landing/styling/responsive, Vitest + Playwright setup.

[ui] Build editor toolbar with formatting buttons — passes=true
  - Created components/EditorToolbar.tsx ("use client") and mounted it in
    components/NoteEditor.tsx at the marked toolbar slot (above EditorContent,
    below the title input). Takes the live `editor: Editor` prop from NoteEditor
    (which already guards `if (!editor) return null`, so editor is non-null here).
    THIS task's whitelist: Bold, Italic, H1, H2, H3, Paragraph(reset). The
    inline-code / code-block / bullet-list / horizontal-rule buttons are the NEXT
    prd task and will be appended to this same toolbar (left a ToolbarButton +
    ToolbarDivider helper to extend). Flagging so a future iter doesn't think the
    toolbar is missing those — they're a separate passes=false task.
  - REACTIVITY (the crux, and the one real integration risk): in @tiptap/react v3
    the editor state is NOT auto-reactive (deliberate perf choice) — a plain
    editor.isActive()/editor.can() read freezes on the INITIAL state, so buttons
    would never highlight. Verified the fix against TipTap v3 docs via context7:
    subscribe through the useEditorState({ editor, selector }) hook; the selector
    re-runs each transaction and re-renders only when a selected value changes.
    Selector returns isBold/canBold/isItalic/canItalic/isH1/isH2/isH3/isParagraph.
    Flagging so a future iter doesn't "simplify" it back to bare isActive() (that
    silently breaks the active highlighting).
  - COMMANDS (SPEC §9.3): bold/italic = editor.chain().focus().toggleBold()/
    toggleItalic().run(); headings = toggleHeading({ level }); paragraph =
    setParagraph() (resets the current block to normal text). Bold/Italic also
    disable when !can(...) (e.g. selection where the mark can't apply).
  - A11y/style: role="toolbar" aria-label, each button type="button" with
    aria-pressed={isActive} + aria-label + title; active = inverted
    bg-foreground/text-background, inactive = hover tint, disabled =
    opacity-40/cursor-not-allowed. Foreground/background theme tokens, dark-mode
    aware, flex-wrap, a thin divider between the marks group and the block group.
    Tailwind v4 utilities only.
  - VERIFIED with Playwright MCP against the live dev server (:3000), existing
    session, opened "Project ideas" (/notes/<uuid>): (1) toolbar renders Bold/
    Italic/H1/H2/H3/Paragraph; on load Paragraph is [pressed] (cursor in the empty
    paragraph) — reactive active state correct from first render; (2) typed text +
    clicked H1 -> text became <h1>, H1 [pressed], Paragraph un-pressed (reactive
    transition); (3) select-all + Bold -> text wrapped in <strong>, Bold [pressed];
    (4) Italic -> <em> added, Italic [pressed]; (5) Paragraph -> the H1 converted
    back to <paragraph> (reset works), Paragraph [pressed], H1 un-pressed. 0
    console errors across the whole flow. NOTE: save isn't wired yet (later task),
    so these edits did NOT persist — the note row is unchanged; dev DB gitignored.
  - `bun run build` PASS (6 routes; /notes/[id] bundles the toolbar into the client
    editor; useEditorState selector typechecks clean), `bun run lint` PASS (clean).
    test/test:e2e unchanged (see CHECKS STATUS — still pre-existing no-suites state;
    those are later prd tasks).
  - NEXT (editor cluster, in order): add code/list buttons to THIS toolbar
    (inline code = toggleCode, code block = toggleCodeBlock, bullet list =
    toggleBulletList, horizontal rule = setHorizontalRule — extend the
    useEditorState selector with their is/can flags) -> save (updateNoteAction,
    debounced auto-save, status indicator, skip initial load — reads NoteEditor's
    title state + contentRef) -> delete (confirm -> deleteNoteAction -> /dashboard)
    -> nav/error/toasts + loading.tsx -> ShareToggle (toggleShareAction) + copy URL.
    Then /p/[slug] public viewer, landing/styling/responsive, Vitest + Playwright.

[ui] Add code and list buttons to toolbar — passes=true
  - Extended components/EditorToolbar.tsx (the existing Bold/Italic/H1-3/Paragraph
    toolbar from the prior iter) with the remaining 4 whitelist controls: Inline
    code (toggleCode), Code block (toggleCodeBlock), Bullet list (toggleBulletList),
    Horizontal rule (setHorizontalRule). All 4 underlying extensions are already
    enabled in lib/tiptap.ts (StarterKit defaults; only orderedList/blockquote/
    strike/link/underline are disabled), so NO lib/tiptap or NoteEditor change was
    needed — purely additive to the toolbar. COMPLETES the editor toolbar.
  - REACTIVITY: extended the SAME useEditorState({ editor, selector }) the toolbar
    already uses (v3 editor state is not auto-reactive) with is/can flags for the
    new buttons — isCode/canCode, isCodeBlock/canCodeBlock, isBulletList/
    canBulletList, and canHorizontalRule. HR is an INSERT not a toggle, so it has
    NO active state (no isHorizontalRule) — only a can() disable check. Flagging so
    a future iter doesn't add a bogus isActive("horizontalRule").
  - GLYPHS (single-glyph aesthetic, matching B/I/H1-3/¶): Inline code "</>" (mono),
    Code block "{ }" (mono), Bullet list "•≡", Horizontal rule "—". Each ToolbarButton
    keeps aria-label + title + aria-pressed for a11y; two new ToolbarDividers group
    marks | blocks | code | list-structure. Reused the existing ToolbarButton/
    ToolbarDivider helpers (no new components).
  - VERIFIED with Playwright MCP against the live dev server (:3000), existing
    session, opened "Untitled note" (/notes/e2adfa59-…): typed "const x = 42;" then
    drove each button + read .tiptap.ProseMirror innerHTML:
      * Inline code: select-all -> click -> <p><code>const x = 42;</code></p>,
        button aria-pressed=true. Toggle off -> back to plain <p>.
      * Code block: click -> <pre><code>const x = 42;</code></pre>. Caret inside ->
        "Code block" [pressed] AND Bold/Italic/Inline code/Bullet list [disabled]
        (the can() enable/disable logic correctly forbids marks/lists in a code
        block). Toggle off -> back to <p>.
      * Bullet list: click -> <ul><li><p>const x = 42;</p></li></ul>, [pressed]=true.
        Toggle off -> back to <p>.
      * Horizontal rule: caret mid-text -> click -> <hr> inserted, splitting the
        paragraph ("con" | <hr> | "st x = 42;"). hasHr=true.
    0 console errors / 0 warnings across the whole flow. NOTE: no Save wired yet
    (later prd task), so these edits did NOT persist — note row unchanged; dev DB
    gitignored.
  - `bun run build` PASS (6 routes; /notes/[id] still ƒ Dynamic, the extended
    useEditorState selector typechecks clean), `bun run lint` PASS (clean).
    test/test:e2e unchanged (see CHECKS STATUS — still pre-existing no-suites state;
    later prd tasks author them).
  - NEXT (editor cluster, in order): save (updateNoteAction, debounced auto-save,
    status indicator saving/saved/error, skip initial load — reads NoteEditor's
    title state + contentRef) -> delete (confirm dialog -> deleteNoteAction ->
    /dashboard) -> nav/error/toasts + app/notes/[id]/loading.tsx -> ShareToggle
    (toggleShareAction, public URL + copy) + copy URL. Then /p/[slug] public viewer
    (@tiptap/static-renderer — NOT installed yet; reuse lib/tiptap `extensions`),
    landing/styling/responsive, then Vitest + Playwright test setup.

[ui] Implement save functionality — passes=true
  - Extended components/NoteEditor.tsx (no new files) to PERSIST the note via the
    existing updateNoteAction Server Action — the editor was display-only until now
    (every prior editor iter noted "edits don't persist yet"). This wires title +
    content through to SQLite. The auth/ownership re-check + the UPDATE stay
    server-side in updateNoteAction; the component only drives the save UX.
  - DEBOUNCED AUTO-SAVE: a single saveTimerRef-based debounce (AUTOSAVE_DELAY_MS =
    1500, within prd's 1–2s). Both the editor's onUpdate (content) and the title
    input's onChange call scheduleSave(), which clears+rearms the timer; 1.5s after
    the LAST edit it fires save(). save() reads the latest title/content from REFS
    (titleRef/contentRef), not state closures, so it always persists current values
    without stale-closure bugs or re-creating callbacks per keystroke.
  - MANUAL SAVE button: calls save() directly, which first clears any pending
    debounce timer so the timer and the click can't double-write. Disabled (+
    "Saving…") while in flight.
  - STATUS INDICATOR (role="status" aria-live="polite"): SaveStatus =
    idle|unsaved|saving|saved|error -> "" / "Unsaved changes" / "Saving…" / "Saved"
    / "Couldn't save" (error in red). prd asked for saving/saved/error; added
    unsaved/idle for clarity (idle = blank on load).
  - NO SAVE ON INITIAL LOAD (prd requirement): scheduleSave is wired ONLY to
    genuine user events (title keystroke / editor transaction). TipTap does NOT
    fire onUpdate for the initial `content` config, and the title input only
    changes on user input, so a freshly loaded note never schedules a save —
    verified empirically (see below). Also clears the pending timer on unmount
    (useEffect cleanup) so navigating away mid-edit can't fire a save into a gone
    component.
  - EMPTY TITLE -> null: save() sends title.trim()===""? null : trimmed, so
    clearing the title clears the column (list falls back to "Untitled note");
    matches UpdateNoteSchema (TitleSchema.nullable, SPEC §5.4 / §13). contentJson =
    contentRef.current (editor.getJSON(), a type:"doc" doc — passes NoteContentSchema
    shape + 1MB cap). updateNoteAction returns ActionResult; ok -> "Saved", else
    "Couldn't save" (status only — no thrown redirect across the boundary).
  - API CLEANUP: removed NoteEditor's now-dead optional onChange/onTitleChange
    props (the page never passed them; the component self-saves now). Only consumer
    is app/notes/[id]/page.tsx, which mounts <NoteEditor note={note} /> unchanged.
    Flagging so a future iter doesn't think those hooks went missing — superseded by
    internal save.
  - VERIFIED with Playwright MCP against the live dev server (:3000), existing
    session, opened an owned note (e2adfa59-…):
      * AUTO-SAVE TRANSITION (recorded via a status MutationObserver while editing
        the title): ["Saved","Unsaved changes","Saving…","Saved"] — the full
        dirty->debounce->persist cycle.
      * PERSISTENCE (reload): title input came back "Transition test 1945" and the
        editor body "Persisted body text from autosave test." — both the title AND
        content auto-saved survived a full page reload (real DB write through
        updateNoteAction, not just client state).
      * NO INITIAL-LOAD SAVE: on a fresh load, initial status = "" and a 2.5s
        MutationObserver saw NO status change with zero interaction
        (statusChangedWithoutEdit: null) — confirms autosave doesn't fire on load.
      * MANUAL SAVE: edit-then-immediate-click went ""->"Saving…"->"Saved"
        (cancelled the pending debounce, wrote once).
      * 0 console errors / 0 warnings across the whole flow.
  - `bun run build` PASS (6 routes; /notes/[id] still ƒ Dynamic — NoteEditor +
    updateNoteAction bundle clean; importing the "use server" action into the
    client editor is the standard pattern, no Turbopack issue), `bun run lint` PASS.
    `bun run test` still exits 1 "No test files found" (pre-existing, NOT a
    regression — the "Write unit tests" prd tasks author those). `bun run test:e2e`
    still N/A (no script; "Set up Playwright" prd task pending).
  - NOTE: the verification wrote title/content to the dev DB data/app.sqlite
    (gitignored) on the e2adfa59 throwaway note. Harmless.
  - NEXT (editor cluster, in order): delete (confirm dialog -> deleteNoteAction ->
    redirect /dashboard + loading state) -> nav/error/toasts + app/notes/[id]/
    loading.tsx -> ShareToggle (toggleShareAction, public URL + copy) + copy URL.
    Then /p/[slug] public viewer (@tiptap/static-renderer — NOT installed yet; reuse
    lib/tiptap `extensions`), landing/styling/responsive, then Vitest + Playwright
    test setup.

[ui] Add delete functionality to editor — passes=true
  - Extended components/NoteEditor.tsx (no new files) with the delete flow that
    wraps the existing deleteNoteAction Server Action. The auth + ownership
    re-check + the SQL stay server-side in the action; the component only drives
    the confirm/loading/redirect UX.
  - CONFIRMATION = native <dialog> (modern accessible browser API, not
    window.confirm): a footer "Delete" button (red text, left of the status+Save
    cluster — footer is now justify-between) calls dialogRef.current.showModal(),
    which gives focus-trapping, Escape-to-close, and a ::backdrop for free. The
    dialog shows "Delete note?" + an interpolated body ("This permanently deletes
    "<title or Untitled note>". This can't be undone.") and Cancel / Delete
    buttons. Chose <dialog> over a window.confirm() so it's styleable + dark-mode
    aware (bg-background/text-foreground tokens, backdrop:bg-black/50). Flagging so
    a future iter doesn't "simplify" it to window.confirm (loses styling/a11y).
  - CONFIRM -> handleDelete (useCallback): cancels any pending auto-save timer
    first (the note is going away — don't let a debounced updateNoteAction write
    into a deleting note), setDeleting(true), await deleteNoteAction(note.id). On
    ok -> router.push("/dashboard") + router.refresh() (refresh purges the now-
    stale list cache so the deleted card is gone); `deleting` stays true through
    the navigation so the buttons can't be re-fired. On error -> setDeleting(false)
    + show result.error inside the still-open dialog (role="alert").
  - LOADING STATE (prd "Show loading state during deletion"): both dialog buttons
    disable while `deleting`; the confirm button label flips to "Deleting…". The
    <dialog> onCancel preventDefault()s while deleting so Escape can't dismiss the
    confirmation mid-action.
  - CANCEL -> dialogRef.current.close() (only when not deleting). Reuses the same
    "use client" component conventions as the save flow (useRef + useState, no
    useEffect for the action). useRouter from next/navigation; deleteNoteAction
    added to the existing import from @/lib/actions/notes.
  - VERIFIED with Playwright MCP against the live dev server (:3000), existing
    session (3 seeded notes kept intact — created a THROWAWAY note to delete):
    (1) New note -> editor; typed title "Delete me 1950" (auto-saved -> "Saved").
    (2) Click Delete -> <dialog> opens, body reads "This permanently deletes
    "Delete me 1950". This can't be undone.", Cancel auto-focused (focus trapped).
    (3) Cancel -> evaluate confirms dialog.open=false, still on /notes/<id> (no
    delete). (4) Re-open -> click confirm Delete -> redirected to /dashboard;
    list back to "3 notes" with the throwaway gone and all 3 seeded notes intact;
    0 console errors across the flow. (5) Navigating to the deleted note's id ->
    404 "This page could not be found." (the 1 console error there is the expected
    404 response, not a code bug) — confirms the row is really gone, not just
    hidden. The "Deleting…" loading label disables the buttons in-flight (fast
    local action, so the transition is brief).
  - `bun run build` PASS (6 routes; /notes/[id] still ƒ Dynamic — NoteEditor +
    deleteNoteAction bundle clean), `bun run lint` PASS (clean). `bun run test`
    still exits 1 "No test files found" (pre-existing, NOT a regression — the
    "Write unit tests" prd tasks author those). `bun run test:e2e` still N/A (no
    script; "Set up Playwright" prd task pending).
  - NOTE: the verification created + deleted a throwaway note in dev DB
    data/app.sqlite (gitignored); the 3 seeded notes are untouched. Harmless.
  - NEXT (editor cluster): nav/error/toasts + app/notes/[id]/loading.tsx (Back link
    already exists on the page; this task adds 404 handling + toast notifications +
    a loading.tsx). Then ShareToggle (toggleShareAction, public URL + copy) + copy
    URL, /p/[slug] public viewer (@tiptap/static-renderer — NOT installed yet;
    reuse lib/tiptap `extensions`), landing/styling/responsive, then Vitest +
    Playwright test setup.

[ui] Add navigation and error handling to editor — passes=true
  (COMPLETES the /notes/[id] editor cluster. 4 sub-steps: Back link (already on the
  page), 404 handling, toasts, loading.tsx — built the 3 missing pieces.)
  - TOAST SYSTEM (new, app-wide + reusable): components/Toast.tsx ("use client") —
    a dependency-free <ToastProvider> + useToast() hook. Provider holds a toasts[]
    array, exposes toast(message, variant?) (variant "success"|"error"), renders a
    fixed bottom-center aria-live region (role="region" aria-label="Notifications")
    of stacked toasts that auto-dismiss after 3s (also a manual ✕). Theme-token
    styled (success = inverted bg-foreground/text-background like the primary
    buttons; error = bg-red-600/white), dark-mode aware. Wired into app/layout.tsx
    wrapping <Header/> + {children}, so it's available on every page (ShareToggle,
    the NEXT task, can reuse it). NOTE: the provider is a client component but takes
    the server-rendered Header/children as `children` props, so pages stay
    server-rendered (no forced "use client" cascade) — build still 6 ƒ routes.
  - EDITOR TOASTS (components/NoteEditor.tsx): added useToast(). save() now takes
    `{ manual }`: a SUCCESS toast ("Note saved") fires ONLY on an explicit Save
    click — auto-save runs ~every 1.5s while typing, so a per-autosave success toast
    would be spam; the existing inline status indicator (Saving…/Saved) covers
    autosave. A FAILURE always toasts (save error + delete error → result.error or a
    fallback). Delete SUCCESS does NOT toast — it redirects to /dashboard and the
    component unmounts before a toast could be seen; the redirect is the feedback.
    Delete failure keeps the user on the page, so it toasts AND keeps the inline
    dialog alert. Flagging the manual-only-success-toast decision so a future iter
    doesn't "fix" autosave to also toast (that's the spam we avoided).
  - GRACEFUL 404 (app/notes/[id]/not-found.tsx, new): the page already calls
    notFound() (missing OR non-owned note — indistinguishable, SPEC §8/§14.2: 404
    not 403). This adds an on-brand 404 segment ("404" / "Note not found" / "This
    note doesn't exist, or you don't have access to it." + a "← Back to dashboard"
    primary button) instead of Next's bare default. Server Component, theme tokens.
  - LOADING (app/notes/[id]/loading.tsx, new): Suspense skeleton shown while the
    dynamic, DB-backed editor page awaits requireAuth()+getNoteById(). Server
    Component mirroring page.tsx/NoteEditor layout EXACTLY (max-w-3xl shell, back-link
    placeholder, right-aligned metadata row, editor card with a title bar, a row of 8
    toolbar-button squares, 5 tapering content lines, footer with Delete+Save
    placeholders) so there's no layout shift; animate-pulse on black/white-alpha
    blocks, aria-busy + sr-only "Loading note…".
  - BACK LINK: "← Back to dashboard" already lives at the top of
    app/notes/[id]/page.tsx (added with the editor-page shell) — this sub-step was
    already satisfied; no change needed.
  - VERIFIED with Playwright MCP against the live dev server (:3000), existing
    session (3 seeded notes): (1) TOAST region present in the DOM on every page
    (dashboard + editor). (2) MANUAL SAVE — opened a note, clicked Save; a
    MutationObserver on the Notifications region captured ["Note saved\n✕"] →
    success toast fires + auto-dismisses. (3) AUTO-SAVE NO-SPAM — reset the observer
    log, edited the title, waited 3s: status indicator went to "Saved" but the toast
    log stayed EMPTY → autosave persists silently (no success toast), exactly as
    designed. (4) GRACEFUL 404 — navigated to /notes/00000000-…-000000000000 →
    rendered the not-found.tsx ("404" / "Note not found" / explanatory copy / Back
    to dashboard link), 0 console errors. (5) LOADING SKELETON — rendered via a TEMP
    host route app/editor-loading-smoke/page.tsx (imported + rendered the loading
    default), screenshotted (editor-loading-smoke.png), confirmed the title/toolbar/
    content/footer skeleton matches the editor layout, then REMOVED the temp route +
    png. 0 console errors across the whole flow.
  - NOTE: the manual-save + title-edit verification wrote to the dev DB
    data/app.sqlite (gitignored) on the e2adfa59 throwaway note. Harmless.
  - `bun run build` PASS (6 routes after temp removal; /notes/[id] still ƒ Dynamic;
    ToastProvider in the layout + NoteEditor toasts bundle clean), `bun run lint`
    PASS (clean, exit 0). `bun run test` still exits 1 "No test files found"
    (pre-existing, NOT a regression — the "Write unit tests" prd tasks author those).
    `bun run test:e2e` still N/A (no script; "Set up Playwright" prd task pending).
  - NEXT: SHARING UI — components/ShareToggle.tsx ("use client", toggle public/
    private via toggleShareAction, show the public /p/[slug] URL when enabled; can
    reuse the new useToast for feedback) → copy-URL (navigator.clipboard + "Copied!"
    feedback) → app/p/[slug]/page.tsx public RSC (getNoteByPublicSlug → notFound) +
    components/PublicNoteViewer.tsx (@tiptap/static-renderer — NOT installed yet;
    reuse lib/tiptap `extensions`). Then landing/styling/responsive, then the Vitest
    + Playwright test-setup tasks.

[testing] Write unit tests for notes repository — passes=true
  - Created __tests__/lib/notes.test.ts: full unit coverage of the lib/notes
    repository surface, running under Vitest on the node:sqlite fallback (same
    proven path as the existing db.test.ts — Vitest runs on Node, not Bun). 12
    tests, all green; whole suite now 3 files / 23 tests pass.
  - SETUP MIRRORS db.test.ts: process.env.DATABASE_PATH=":memory:" set BEFORE the
    first getDb() (lazy open reads it once). beforeEach closeDb() for a clean slate,
    then seedUsers() creates a minimal stub `"user"` table (just the id PK the
    notes FK targets) + alice/bob rows — the bare in-memory DB has no better-auth
    `user` table, and PRAGMA foreign_keys=ON means inserts need the FK target to
    exist. notes table itself is auto-created by getDb()->applyNotesSchema.
  - COVERS every prd step + the authorization invariants (SPEC §8/§14.2):
    * createNote(): defaults (title "Untitled note", empty TipTap doc, is_public
      0/null slug, created_at===updated_at), row persisted with content as a JSON
      *string* (SPEC §5.3), honors provided title/content, unique UUID per note.
    * getNoteById(): owner fetch works; non-owner + missing id BOTH -> null
      (404-not-403, existence not revealed). getNotesByUser(): per-user scoping +
      updated_at DESC order (forced timestamps so the ms-tie can't flake).
    * updateNote(): partial patch + ALWAYS bumps updated_at (asserted against a
      forced known-old baseline so the bump is deterministic, no ms tie); untouched
      field preserved; non-owner -> null + note unchanged.
    * deleteNote(): non-owner -> false + note intact; owner -> true + gone;
      re-delete -> false (idempotent).
    * setNotePublic()/getNoteByPublicSlug(): enable mints a 21-char slug + public
      lookup works; disable KEEPS the slug (SPEC §10.2 — NOT cleared as the prd
      step loosely words it; test comments flag this so a future iter doesn't
      "fix" it) but lookup 404s via the is_public=1 gate; re-enable reuses the same
      slug; non-owner -> null; unknown slug -> null.
  - DETERMINISM: anywhere a timestamp comparison mattered (list order, update
    bump) the test forces explicit updated_at values via raw SQL rather than
    relying on wall-clock ms gaps (createNote uses ms-resolution ISO strings that
    can tie within a test) — so the suite is flake-free.
  - No `any` (typed StoredNote for the raw-row asserts), no unused imports.
  - VERIFIED: `bun run test` PASS (3 files / 23 tests), `bun run lint` PASS (clean),
    `bun run build` PASS (7 routes — graph unchanged; tests aren't bundled). No UI
    in this task -> Playwright N/A. `bun run test:e2e` still N/A (no script; the
    "Set up Playwright" prd task is still pending).
  - NEXT (testing): "Write unit tests for server actions" (mock auth session +
    revalidatePath / next-cache; lib/actions/notes.ts) then "Set up Playwright for
    E2E testing" (the risky integration: @playwright/test + browser install +
    webServer config) which unblocks the 4 E2E spec tasks. Also still open: 3
    styling/responsive UI tasks.

--------------------------------------------------------------------------------
CHECKS STATUS (whole repo)
--------------------------------------------------------------------------------
  bun run build     : PASS
  bun run lint      : PASS
  bun run test      : PASS — 4 files / 37 tests. __tests__/setup.test.ts (2,
                      jsdom + "@/" alias), __tests__/lib/db.test.ts (9, generic
                      helpers), __tests__/lib/notes.test.ts (12, repository surface
                      + authz), __tests__/actions/notes.test.ts (14, server-action
                      guards — auth/ownership/validation/revalidate). The UNIT-TEST
                      TRILOGY is now COMPLETE (db + notes + actions).
  bun run test:e2e  : PASS — 1 file / 3 smoke tests (e2e/smoke.spec.ts) on the
                      isolated test server (:3100 + ./data/test.sqlite + .next-e2e).
                      Playwright harness is now SET UP; the feature specs
                      (auth/notes/sharing/responsive) are the remaining testing tasks.

--------------------------------------------------------------------------------
NEXT / NOTES FOR FUTURE ITERATIONS
--------------------------------------------------------------------------------
  - DATABASE CATEGORY FULLY COMPLETE (all 9 db tasks passes=true): data layer
    (lib/notes.ts) full repository surface (create/getById/getByUser/update/
    delete/setNotePublic/getNoteByPublicSlug) + types/helpers, AND the schema
    task (verified this iter — user/session/account/verification + notes all
    created on cold start). AUTH CATEGORY ALSO COMPLETE (server, route, helpers,
    client all passes=true). Next phases: auth UI -> actions -> CRUD/editor/share
    UI -> tests.
  - AUTH ROUTE DONE this iter (app/api/auth/[...all]/route.ts) — endpoints live
    and verified. Remember the Turbopack lesson: any NEW route/page that imports
    lib/db (directly or via lib/auth) must build clean; the getBuiltinModule
    driver load already handles that, so don't "fix" it back to a dynamic require.
  - Suggested next (in rough dependency order) — db + auth foundation now ALL
    done, so the frontier is auth UI then actions then the rest of the UI:
      1. AUTH UI: components/SignUpForm.tsx + components/LoginForm.tsx ("use
         client", import signUp/signIn from "@/lib/auth-client", controlled
         state, client-side validation, error display) -> app/authenticate/page.tsx
         (toggles the two forms, redirects to /dashboard on success) -> Header
         (server component, getSession()) + LogoutButton ("use client", signOut).
         NOTE Turbopack: any NEW route/page importing lib/db (directly or via
         lib/auth, e.g. Header) must build clean — getBuiltinModule already
         handles it, don't revert to a dynamic require.
      2. ACTIONS: lib/actions/notes.ts ("use server") wrapping the lib/notes repo
         fns; guard order session -> ownership -> Zod -> SQL -> revalidatePath.
         These unblock CreateNoteButton, the editor save/delete, and ShareToggle.
      3. THEN CRUD/editor/share UI (dashboard, /notes/[id] editor + TipTap,
         /p/[slug] public viewer) and finally the test tasks (Vitest + Playwright).
         Server helpers DONE: getSession()/requireAuth() in lib/auth.ts;
         requireAuth() redirects to /authenticate (prd route).
  - DONE this iter: "Implement server-side auth helpers" — getSession()/
    requireAuth() in lib/auth.ts (see DONE section). requireAuth redirects to
    /authenticate (prd route), returns NonNullable<Session>.
  - REPO-FN CONVENTION (set by createNote this iter): createNote is async per prd;
    the read/update/delete/share fns can stay SYNC (driver is sync). All return
    Note | Note[] | boolean and bake ownership into the WHERE clause. Reuse
    run()/get()/query() from lib/db and mapRowToNote() from lib/notes.
  - lib/db.ts deliberately does NOT create any tables; auth tables come from
    better-auth migrations (now wired in lib/auth.ts on cold start).
  - When wiring the notes table, follow SPEC §5 (notes columns) and reuse
    getDb()/run().
  - Vitest runs under Node, so keep server data code driver-agnostic (the
    node:sqlite fallback in lib/db.ts handles this). NOTE: better-auth itself is
    backed by bun:sqlite under the app runtime; do NOT try to drive auth flows
    from vitest/Node — verify auth via Bun (smoke script or e2e) instead.

[ui] Build share toggle component + Add copy URL functionality — passes=true (x2)
  (Did ONE focused increment — components/ShareToggle.tsx — that completes the
  "Build share toggle component" task AND its tightly-coupled successor "Add copy
  URL functionality" (whose every step is a sub-part of building the share UI).
  Both verified end-to-end via Playwright, so both flipped to passes=true rather
  than leaving copy-url falsely false. Opens the SHARING feature; unblocks the
  /p/[slug] public page (you now have a real public slug to test against).)
  - Created components/ShareToggle.tsx ("use client"): the per-note sharing control
    (SPEC §10.2). Props { noteId, initialIsPublic, initialPublicSlug }. Drives the
    UX only — the authoritative auth+ownership re-check and SQL stay server-side in
    toggleShareAction (SPEC §8/§12/§14.2). Local useState mirrors the server so the
    switch flips instantly; the action's returned Note keeps it in sync.
  - TOGGLE: accessible switch — a real <button role="switch" aria-checked> (keyboard
    + SR friendly), green when on. onClick -> toggleShareAction(noteId, !isPublic);
    on ok, adopts result.data.isPublic/publicSlug + success toast; on error, error
    toast (reuses the app-wide useToast from components/Toast). Disabled while pending.
  - PUBLIC URL: shown only when public. Built as `${NEXT_PUBLIC_APP_URL}/p/${slug}`
    (SPEC §15) from the SLUG, never the internal note id (SPEC §10.1/§14.5). Empty
    env fallback yields a relative /p/<slug> (still copies/opens). Read-only input,
    selects-all on focus.
  - COPY (the copy-url task): navigator.clipboard.writeText(shareUrl) -> button label
    flips "Copy link" -> "Copied!" for 2000ms (COPIED_FEEDBACK_MS), then reverts.
    clipboard can reject (denied/insecure context) -> caught -> error toast (graceful,
    not swallowed). No success toast on copy (the inline "Copied!" is the feedback).
  - WIRING: app/notes/[id]/page.tsx renders <ShareToggle> below <NoteEditor>, passing
    note.id/isPublic/publicSlug. The page's static Public/Private header badge stays
    (reflects server state on next load); ShareToggle is the interactive surface.
    ToastProvider already wraps the app in app/layout.tsx, so useToast works here.
  - SLUG SEMANTICS (delegated to setNotePublic, SPEC §10.2): enable mints a slug only
    if none exists (re-share reuses the SAME link); disable keeps the slug but flips
    is_public=0 so old links 404 via the is_public=1 gate. Confirmed by Playwright
    (toggle off then the link is gone from UI; slug persists in DB for re-share).
  - VERIFIED with Playwright MCP against the live dev server (:3000), existing
    session: opened a Private note -> Sharing section shows switch OFF + "Only you can
    see this note." + NO url. Flip ON -> switch checked + "Anyone with the link can
    view this note." + url input "http://localhost:3000/p/C6C6RuxeIlxQs-KzbSZ_z" +
    Copy link button + header badge flipped Public. Reloaded -> still public (DB
    write persisted via toggleShareAction). Copy: in this sandboxed context
    navigator.clipboard is permission-denied (NotAllowedError) -> the FAILURE path
    held correctly (button stayed "Copy link"); then stubbed a resolving writeText
    to exercise the SUCCESS path -> writeText received the exact share URL AND the
    label transitioned to "Copied!" (observed via in-page click+settle). Toggle OFF
    -> switch unchecked, url GONE, message back to "Only you can see this note.".
    0 console errors across the whole flow.
  - CHECKS: `bun run lint` PASS (clean), `bun run build` PASS (6 routes; /notes/[id]
    compiles with ShareToggle as ƒ dynamic). `bun run test` = "No test files found"
    exit 1 — EXPECTED, the Vitest setup + test tasks are still passes=false (not a
    regression; testing category not started). No `test:e2e` script exists yet
    (Playwright-test setup is also a pending prd task; UI verified via Playwright MCP
    instead, per the loop instruction).
  - NOTE: the verify left the "autosave" note PRIVATE (toggled back off); its slug is
    retained per SPEC §10.2. Writes hit the gitignored dev DB data/app.sqlite. Harmless.
  - NEXT: the public read path — app/p/[slug]/page.tsx (RSC, getNoteByPublicSlug, 404
    if missing/not public, NO auth) + components/PublicNoteViewer.tsx rendering the
    TipTap JSON server-side with @tiptap/static-renderer's renderToReactElement
    (React-escaped, no dangerouslySetInnerHTML — SPEC §10.3/§14.1), reusing the shared
    lib/tiptap extensions. @tiptap/static-renderer is NOT in package.json yet -> bun add
    it. This is the riskiest remaining integration (new dep + the shared-config render
    contract); do it next. Then landing/styling/responsive, then the test setup tasks.

[ui] Build public note page with RSC + Create public note viewer component — passes=true (x2)
  (Did ONE focused increment — the public read path — that completes BOTH tightly-
  coupled tasks: the page's whole job is to fetch + render via the viewer, and the
  viewer's whole job is to render the note's JSON. Both verified end-to-end via
  Playwright, so both flipped to passes=true (ShareToggle precedent). This is the
  riskiest remaining integration: a NEW dep + the shared-config server-render contract.)
  - NEW DEP: `bun add @tiptap/static-renderer@^3.27.1` -> resolved 3.27.1 (matches the
    @tiptap/core 3.27.1 override + react/starter-kit ^3.27.1). The page bundles it +
    pulls lib/notes->lib/db into a PAGE bundle (getBuiltinModule driver — builds clean;
    don't revert to a dynamic require). Confirmed the export subpath via the installed
    package.json exports + context7: `import { renderToReactElement } from
    "@tiptap/static-renderer/pm/react"`, signature renderToReactElement({ content,
    extensions }) -> ReactElement (matches SPEC §10.3 exactly).
  - components/PublicNoteViewer.tsx: SERVER COMPONENT (no "use client"). Renders the
    stored TipTap JSON to React server-side via renderToReactElement with the SHARED
    `extensions` from lib/tiptap (CLAUDE.md invariant — same config as the editor so
    node/mark mappings line up). Output is React-ESCAPED (no dangerouslySetInnerHTML),
    the core XSS defence (SPEC §14.1). Props { title, contentJson } ONLY — the internal
    note id is never passed to / rendered on the public route (SPEC §10.1/§14.5). Title
    as <h1> (falls back "Untitled note"); body in <div class="prose dark:prose-invert
    max-w-none"> for reading typography. NO editor / NO toolbar / NO editing controls.
  - PRD-vs-SPEC RESOLVED: the prd step says "render with editable:false" (which would
    mean a CLIENT @tiptap/react editor). Followed the AUTHORITATIVE CLAUDE.md invariant
    + SPEC §10.3 instead — server-side STATIC render, no editor, no DOM. Documented in
    the component header so a future iter doesn't "fix" it back to a client editor.
  - app/p/[slug]/page.tsx: async Server Component, NO auth (the only note route without
    it). params: Promise<{ slug }> (Next 16 async). getNoteByPublicSlug(slug) — gated
    SOLELY on is_public=1 (CLAUDE.md invariant), looked up by the share TOKEN never the
    id -> null when missing/private -> notFound() (404 not 403, never reveals existence).
    Renders <PublicNoteViewer title contentJson/>. Reads happen directly in the RSC
    (SPEC §3.1 — no REST API).
  - VERIFIED with Playwright MCP against the live dev server (:3000). Seeded a PUBLIC
    note via a throwaway bun script (_seed-public.ts, REMOVED after) with rich content
    covering EVERY whitelisted node/mark (h1, h2, bold/italic/inline-code, bulletList x2,
    codeBlock, horizontalRule + a literal "<script>alert(1)</script>" string), wrote to
    the gitignored dev DB. Checks:
      (1) RENDER — /p/<slug> accessibility snapshot showed title h1 "Shared public note"
          + the full doc: heading[1], heading[2], paragraph with strong/emphasis/code,
          list+2 listitems, code block "const answer = 42;", separator (hr), final
          paragraph. The "<script>alert(1)</script>" rendered as LITERAL TEXT, not a
          DOM <script> — proves React-escaping. NO editor/toolbar (read-only). 0 console
          errors on the page.
      (2) ANONYMOUS (no-auth) — fetch with credentials:'omit' -> 200 + HTML contains
          "Public note heading" -> the public route needs NO session. Also confirmed the
          raw HTML has "&lt;script&gt;" and NOT "<script>alert(1)" (XSS-safe on the wire).
      (3) BAD SLUG — fetch /p/this-slug-does-not-exist -> 404.
      (4) PRIVATE-BUT-SLUGGED (the is_public=1 gate, security-critical) — toggled the
          seeded note private (slug RETAINED per SPEC §10.2) via _unshare-check.ts
          (REMOVED), then fetched its slug -> 404 + HTML does NOT contain the content.
          Confirms an unshared note 404s even though its slug still exists.
    Both throwaway scripts + the screenshot removed after.
  - CHECKS: `bun run build` PASS (7 routes; /p/[slug] now ƒ Dynamic — correct, it reads
    the DB per request), `bun run lint` PASS (clean, exit 0). `bun run test` still exits
    1 "No test files found" (pre-existing, NOT a regression — the "Write unit tests" prd
    tasks author the files; testing category not started). `bun run test:e2e` still N/A
    (no script; "Set up Playwright" prd task pending).
  - NOTE: the seed wrote a note row to the gitignored dev DB data/app.sqlite (left
    PRIVATE after the unshare check; its slug is retained per SPEC §10.2). Harmless.
  - NEXT: the SHARING feature is now end-to-end (toggle -> public URL -> public read
    page). Remaining UI is presentational: "Style the landing page" (app/page.tsx is
    still the default Next skeleton), "Apply consistent styling and dark mode",
    "Implement responsive design". Then the TESTING category (all passes=false): Vitest
    config -> unit tests (db/notes/actions) -> Playwright setup -> e2e specs (auth/notes/
    sharing/responsive). The data + actions + repo layers already have throwaway-smoke
    coverage that the Vitest tasks can formalize.

[testing] Set up Vitest configuration — passes=true   (FIRST testing task; opens the
  TESTING category. Chose this risky/foundation task over the routine presentational
  UI tasks per the loop rule "foundation/risky before routine" — it unblocks all 3
  unit-test tasks (db/notes/actions).)
  - Created vitest.config.ts (the only deliverable; vitest@4.1.9 + jsdom@29 +
    @testing-library/react + @vitejs/plugin-react were ALREADY in devDeps from a
    prior setup, and package.json already had test scripts: test=`vitest run`,
    test:watch=`vitest`). Config: plugins:[react()] (JSX/React 19 transform);
    test.environment:"jsdom" (prd step 1 — enables the later component tests; the
    Node builtins the data layer needs still load fine under jsdom, see below);
    test.include:["__tests__/**/*.{test,spec}.{ts,tsx}"] (prd step 3 — where the
    db/notes/actions suites will live; node_modules excluded by Vitest's default
    exclude, which matters since zod/tsconfig-paths ship their own *.test.ts);
    resolve.alias "@" -> project root via dirname(fileURLToPath(import.meta.url))
    (prd step 2 — mirrors tsconfig's "@/*", so tests import `@/lib/...` like the app).
  - RUNTIME NOTE (the real "risky integration" this de-risks for the next tasks):
    Vitest runs under NODE, not Bun. lib/db.ts is driver-agnostic and falls back to
    node:sqlite (process.getBuiltinModule) under Node — Node here is v24.17.0, which
    has node:sqlite (needs >=22.5) — so the repository/action suites will be testable
    without a Bun process. jsdom doesn't touch `process`/Node builtins, so node:sqlite
    still loads under the jsdom environment. (This task's own test doesn't hit the DB.)
  - GLOBALS DECISION: did NOT set test.globals:true. Test files import { describe, it,
    expect } from "vitest" explicitly. WHY: tsconfig.json includes **/*.ts, so
    `next build` typechecks the __tests__ files too — bare globals would need
    vitest/globals wired into the TS types (extra tsconfig surgery); explicit imports
    keep test files valid TS under the Next build with zero tsconfig changes. Confirmed:
    `bun run build` typechecked __tests__/setup.test.ts cleanly. Flagging so a future
    iter doesn't switch to globals without also fixing the build typecheck.
  - VERIFY (prd step 4 "verify runs correctly"): added __tests__/setup.test.ts — a
    minimal sanity suite proving the config actually works: (1) jsdom env active
    (typeof window/document === "object"), (2) the "@/" alias resolves a real module
    (imports @/lib/validation's TitleSchema, asserts it trims). NOTE: ran via
    `bun run test` (= `vitest run`), NOT bare `bun test` (Bun's own runner would
    conflict with Vitest); the prd's "bun test" wording means the npm script. With no
    test files `vitest run` exits 1 ("No test files found"), so this sanity suite is
    what makes the verification real — and it's a legit permanent part of the test
    setup. The later db/notes/actions suites add their own files alongside it.
  - CHECKS: `bun run test` PASS (1 file, 2 tests, ~1.6s) — FIRST passing run, flips the
    long-standing "No test files found" exit-1 state. `bun run build` PASS (7 routes,
    all ƒ Dynamic; typechecks the new test file), `bun run lint` PASS (clean, exit 0).
    `bun run test:e2e` still N/A (no script; "Set up Playwright for E2E testing" is a
    later pending task). No UI/app change in this task -> Playwright MCP N/A.
  - NEXT (testing, in dependency order): the 3 unit-test tasks now unblocked —
    __tests__/lib/db.test.ts (getDb singleton + query/get/run; use DATABASE_PATH=
    :memory: + a stub `user` table for FKs, same as the prior throwaway smokes),
    __tests__/lib/notes.test.ts (createNote defaults / getNoteById authz / updateNote
    timestamp / deleteNote authz / setNotePublic slug), __tests__/actions/notes.test.ts
    (mock the auth session, then create/update/delete/toggleShare). All run under Node/
    node:sqlite — do NOT try to drive better-auth flows from Vitest (it's bun:sqlite-
    backed under the app runtime; verify auth via Bun smoke or Playwright e2e instead).
    Then "Set up Playwright for E2E testing" (install @playwright/test + browsers, add a
    test:e2e script + playwright.config.ts with a webServer) and the e2e specs. The
    presentational UI tasks (landing/styling/responsive) remain too.

[testing] Write unit tests for database utilities — passes=true
  - Created __tests__/lib/db.test.ts: 10 tests over lib/db.ts's generic helpers,
    the first of the 3 unit-test suites the Vitest config unblocked. Formalizes the
    throwaway "dual-runtime smoke" the lib/db.ts iter ran. Runs under Node (Vitest)
    via the node:sqlite fallback (Node v24.17.0 has node:sqlite; $name binds work —
    verified directly). Chose this foundation test over the risky Playwright setup /
    routine presentational UI per the loop rule + prd dependency order.
  - ISOLATION: top-of-file `process.env.DATABASE_PATH = ":memory:"` (set before the
    first lazy getDb(), so no file is written). beforeEach closeDb() + recreate a
    throwaway `items` table — a :memory: DB lives only as long as its connection, so
    closeDb() gives each test a clean slate AND clears the prepared-stmt cache (no
    statement bound to a closed handle leaks). afterAll closeDb(). Used a dedicated
    `items` table (not `notes`) so the suite doesn't need the better-auth `user`
    table that notes' FK targets.
  - COVERAGE (prd steps): getDb() singleton (getDb()===getDb(); fresh instance after
    closeDb()); run() INSERT (changes=1 + numeric lastInsertRowid>0) / UPDATE (multi-
    row change count) / DELETE (change count + row gone); query<T>() (all rows typed,
    $name bind filters, [] when no match); get<T>() (single row, null when no match —
    confirms lib/db's `row ?? null` normalization of node:sqlite's undefined).
  - GOTCHA FIXED (flagging so a future iter doesn't reintroduce): comparing the two
    native DB handles via `expect(second).not.toBe(first)` THREW "database is not
    open". Vitest's toBe runs a deep-equality "did you mean toEqual?" hint that
    traverses the operands; traversing the now-CLOSED `first` handle throws. Fix:
    reduce identity to a plain boolean first — `expect(Object.is(first, second))
    .toBe(false)` — so the matcher never deep-traverses a closed native handle.
    Don't switch it back to `.toBe(handle)` on a closed handle.
  - CHECKS: `bun run test` PASS (2 files, 12 tests — setup.test.ts 2 + db.test.ts 10,
    ~1.3s). `bun run build` PASS (7 routes; tsconfig includes **/*.ts so the new test
    file is typechecked by the Next build — clean). `bun run lint` PASS (clean, exit
    0). `bun run test:e2e` still N/A (no script; "Set up Playwright" prd task pending).
    No UI/app change -> Playwright MCP N/A.
  - NEXT (testing, in dependency order): __tests__/lib/notes.test.ts (createNote
    defaults / getNoteById authz / updateNote timestamp bump / deleteNote authz /
    setNotePublic slug mint+clear-gate — needs a stub `user` table + row for the FK,
    same :memory: pattern as here) -> __tests__/actions/notes.test.ts (mock the auth
    getSession, then create/update/delete/toggleShare). All under Node/node:sqlite —
    do NOT drive better-auth flows from Vitest (bun:sqlite-backed under the app
    runtime; use a Bun smoke or Playwright e2e for auth). Then "Set up Playwright for
    E2E testing" (install @playwright/test + browsers, add a test:e2e script +
    playwright.config.ts webServer) and the e2e specs. Presentational UI tasks
    (landing/styling/responsive) also remain.

[testing] Write unit tests for server actions — passes=true
  (COMPLETES the unit-test trilogy: db + notes + actions all passes=true. Chose
  this foundation task over the risky Playwright setup / routine presentational UI
  per the loop rule + prd dependency order — it formalizes coverage of the most
  security-critical layer, the action guards.)
  - Created __tests__/actions/notes.test.ts: 14 tests over lib/actions/notes.ts's
    four server actions (create/update/delete/toggleShare). Runs under Node (Vitest)
    against a REAL in-memory DB through the repo layer — the actions exercise their
    real guard order (session -> ownership -> Zod -> SQL -> revalidate), not stubs.
  - MOCKING (the crux): vi.mock("@/lib/auth", () => ({ getSession: vi.fn() })) so
    (a) getSession is controllable per test (signInAs/signOut helpers drive its
    resolved value) and (b) importing the actions NEVER loads the real lib/auth —
    which runs better-auth's migrations in a TOP-LEVEL await against bun:sqlite,
    unavailable under Vitest/Node (the standing "don't drive better-auth from
    Vitest" warning). Also vi.mock("next/cache", () => ({ revalidatePath: vi.fn()}))
    — revalidatePath throws outside a Next request scope; the mock lets the actions
    run here AND lets us assert which paths each mutation purges. The repo layer
    (lib/notes -> lib/db, node:sqlite fallback) is deliberately NOT mocked so the
    DB/authz behavior is real. Flagging so a future iter doesn't "simplify" by
    mocking lib/notes (that would gut the integration value) or un-mock lib/auth
    (that reintroduces the bun:sqlite top-level-await import hazard under Node).
  - ISOLATION: same :memory: pattern as the db/notes suites — DATABASE_PATH=:memory:
    set before the first getDb(); beforeEach does vi.clearAllMocks() + closeDb() +
    seedUsers() (stub `user` table + alice/bob for the FK + cross-owner authz).
  - COVERAGE (prd steps): createNoteAction — signed-in creates + persists + owner
    scoping + revalidates /dashboard; honors title/content; unauthenticated ->
    error + no DB touch; invalid contentJson (type!="doc") -> "Invalid note data.".
    updateNoteAction — owner update applies + revalidates /dashboard + /notes/:id;
    NON-OWNER -> "Note not found." + note untouched + no revalidate (404-not-403);
    unauthenticated -> error. deleteNoteAction — owner deletes + gone + revalidate;
    non-owner -> "Note not found." + note survives; unauthenticated -> error.
    toggleShareAction — enable mints slug + getNoteByPublicSlug reachable +
    revalidates /p/:slug; disable KEEPS slug but public lookup 404s (is_public=1
    gate, SPEC §10.2); non-owner -> error + stays private; unauthenticated -> error.
  - No `any` (session built + cast once via SessionResult helper), no unused imports.
  - VERIFIED: `bun run test` PASS (4 files / 37 tests; +14 from this file),
    `bun run lint` PASS (clean), `bun run build` PASS (7 routes — graph unchanged,
    tests aren't bundled; tsconfig includes **/*.ts so the build also typechecked
    the new test file clean). No UI in this task -> Playwright MCP N/A.
    `bun run test:e2e` still N/A (no script; "Set up Playwright" prd task pending).
  - NEXT (testing): "Set up Playwright for E2E testing" — the risky integration:
    bun add -d @playwright/test + `bunx playwright install` browsers, add a test:e2e
    script + playwright.config.ts with a webServer (bun --bun next dev/start) on
    Windows (watch for the flagged Bun+Next dev-server flakiness), e2e/ dir +
    auth/db helpers. It unblocks the 4 e2e spec tasks (auth/notes/sharing/responsive).
    Also still open: 3 presentational UI tasks (landing/styling/responsive).

[testing] Set up Playwright for E2E testing — passes=true
  (The risky integration — chosen over the 3 routine presentational UI tasks per
  the loop rule "foundation/risky before routine". UNBLOCKS all 4 e2e spec tasks
  (auth/notes/sharing/responsive). @playwright/test@1.61.1 + chromium were already
  in devDeps/cache; this iter built the config, helpers, DB-reset, test:e2e script,
  and a 3-test smoke that proves the whole harness end-to-end.)
  - ISOLATION (the core design): the E2E server is fully decoupled from local dev,
    so it can run alongside the :3000 dev server AND in CI without contention:
      * PORT 3100 (dev is 3000) — set via the webServer `env`.
      * DATABASE_PATH=./data/test.sqlite — a dedicated, disposable SQLite file
        (lib/db.ts reads DATABASE_PATH; .gitignored under /data/).
      * NEXT_DIST_DIR=.next-e2e — its OWN Turbopack build dir. Added an env-driven
        `distDir` to next.config.ts (`...(process.env.NEXT_DIST_DIR ? {distDir} :
        {})`). WHY: two `next dev` (Turbopack) servers sharing `.next` is flaky on
        Windows. A normal `bun run build`/`dev` leaves NEXT_DIST_DIR unset -> default
        `.next`, unchanged. Flagging so a future iter doesn't drop the distDir spread.
      * BETTER_AUTH_URL + NEXT_PUBLIC_APP_URL -> http://localhost:3100 so better-auth's
        origin check passes on the sign-up POST and share links point at the test port.
    BETTER_AUTH_SECRET still comes from .env (Next loads it; webServer `env` only
    overrides the 4 vars above — Playwright merges `env` onto process.env, and Next's
    @next/env does NOT override already-set process.env, so the overrides win).
  - playwright.config.ts: testDir ./e2e; ONE chromium project (devices Desktop
    Chrome); fullyParallel:false + workers:1 (SQLite is single-writer — serial avoids
    "database is locked" contention, the flagged WAL hazard); timeout 60s (cold
    Turbopack route compiles + first-request auth migrations are slow); webServer
    `command: "bun run dev"`, url :3100, reuseExistingServer:!CI, 120s startup timeout,
    + the `env` block above. trace on-first-retry; retries 2 on CI.
  - DB RESET (e2e/setup.ts) — the tricky part, two findings worth remembering:
      1. Playwright launches the webServer BEFORE globalSetup runs. The server's cold
         start opens test.sqlite (lib/auth migrations on import, triggered by the
         readiness GET /), so by the time a globalSetup runs the WAL files are LOCKED
         -> rmSync threw EPERM. CONCLUSION: a Playwright globalSetup is TOO LATE to
         reset the DB. Reset must happen BEFORE `playwright test`.
      2. Playwright's globalSetup loader compiles TS to CJS, where `import.meta` is
         illegal ("Cannot use 'import.meta' outside a module") — so a file using
         import.meta CANNOT also be wired as globalSetup.
    RESOLUTION: e2e/setup.ts exports resetTestDb() (rmSync the 3 test.sqlite* files,
    force:true ignores missing, swallow EPERM/EBUSY) and self-runs via
    `if ((import.meta as {main?}).main) resetTestDb()`. It is NOT a globalSetup; the
    `test:e2e` script runs it pre-launch: `bun e2e/setup.ts && playwright test`. Under
    bun, import.meta.main works; nothing imports it as CJS, so no import.meta error.
    Flagging both findings so a future iter doesn't "restore" globalSetup wiring
    (reintroduces the import.meta crash) or rely on globalSetup for the reset (too late).
  - e2e/helpers/auth.ts: signUp(page, creds) (toggle to sign-up, fill Name/Email/
    Password via getByLabel, submit, waitForURL **/dashboard) + login(page, creds)
    + uniqueEmail()/newCredentials() (timestamp+random emails so re-runs never collide
    even on a non-pristine DB). 60s redirect timeout for cold compiles. Selectors:
    the only "Sign up" control in login mode is the toggle; after toggling the only
    "Sign up" is the submit — both getByRole("button",{name:"Sign up"}) calls are
    unambiguous at their point of use.
  - e2e/smoke.spec.ts: 3 tests proving the harness — landing renders (Header "Notes"
    link), /authenticate renders (heading "Welcome back" + "Log in" button), and a
    NEW USER CAN SIGN UP -> /dashboard (uses the signUp helper -> asserts the "Log out"
    button = active session). The last test exercises the full webServer+test-DB+auth
    POST path, de-risking the later auth/notes specs.
  - CONFIG HYGIENE (both REQUIRED — a generated .next-e2e dir breaks tooling that only
    knew about .next): eslint.config.mjs globalIgnores += .next-e2e/**, test-results/**,
    playwright-report/** (without this, eslint lints the generated .next-e2e -> 6390
    problems). .gitignore += /.next-e2e/ + the playwright artifact dirs. tsconfig's
    `**/*.ts` include still typechecks .next-e2e cleanly (build verified), so no
    tsconfig change needed. Flagging so a future iter keeps these ignores.
  - VERIFIED: `bun run test:e2e` PASS (3/3, ~10s, clean — no EPERM noise after the
    pre-launch reset). `bun run test` PASS (4 files/37 — unit suite unaffected).
    `bun run lint` PASS (clean after the .next-e2e ignore). `bun run build` PASS (7
    routes; typechecks the new e2e/*.ts + playwright.config.ts + setup.ts). NOTE: one
    build transiently failed with the FLAGGED WAL hazard ("Failed to collect page data
    for /api/auth" — 15 workers cold-starting auth contend with the live :3000 dev
    server); a plain re-run PASSED. If a future build fails on /api/auth page data,
    just re-run.
  - NEXT (testing): the 4 e2e spec tasks are now unblocked — e2e/auth.spec.ts (sign
    up / log in / invalid creds / redirect / logout — reuse the auth helpers),
    e2e/notes.spec.ts (create/edit/format/autosave/delete), e2e/sharing.spec.ts
    (toggle public, public URL, anon access, 404 for private/invalid, copy), and
    e2e/responsive.spec.ts (mobile/tablet/dark mode). Each should sign up a fresh
    user via the helper (clean isolation). Also still open: 3 presentational UI tasks
    (landing/styling/responsive).
